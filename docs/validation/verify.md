# Verification tracker

Every model parameter cites a published source in its data file, or is marked
`# PLACEHOLDER — verify against <source>` and listed here until it does.

## Parameters in the model

Every number below is in the code or in a test fixture today, with the source it
came from. Anything not on this list and not marked PLACEHOLDER is a bug.

| Parameter | Value | Source | Used in |
|---|---|---|---|
| Solar constant Gsc | 0.0820 MJ m⁻² min⁻¹ | [FAO-56 Eq. 21](https://www.fao.org/4/x0490e/x0490e07.htm) | `core/Solar.cpp` |
| Inverse Earth-Sun distance dr | 1 + 0.033 cos(2πJ/365) | FAO-56 Eq. 23 | `core/Solar.cpp` |
| Solar declination δ | 0.409 sin(2πJ/365 − 1.39) | FAO-56 Eq. 24 | `core/Solar.cpp` |
| Sunset hour angle ωs | arccos(−tan φ tan δ) | FAO-56 Eq. 25 | `core/Solar.cpp` |
| Radiation to evaporation | 0.408 mm per MJ m⁻², λ = 2.45 MJ kg⁻¹ | FAO-56 Eq. 20 | `core/Solar.cpp` |
| Hargreaves ETo | 0.0023 (Tmean + 17.8) √(Tmax − Tmin) Ra | FAO-56 Eq. 52 | `core/SoilWater.cpp` |
| Total available water TAW | 1000 (θFC − θWP) Zr | [FAO-56 Eq. 82](https://www.fao.org/4/x0490e/x0490e0e.htm) | `core/SoilWater.cpp` |
| Water stress Ks | (TAW − Dr) / (TAW − p·TAW) | FAO-56 Eq. 84 | `core/SoilWater.cpp` |
| Depletion adjustment | p = p_table + 0.04 (5 − ETc), clamped to [0.1, 0.8] | FAO-56 Table 22 note | `core/SoilWater.cpp` |
| Depletion fraction p, grazed pasture | 0.60 | FAO-56 Table 22 (rotated and extensive grazing) | test fixtures |
| Crop coefficient Kc, rotated grazing | 0.85–1.05 mid-season; 0.95 used | [FAO-56 Table 12](https://www.fao.org/4/x0490e/x0490e0b.htm) | test fixtures |
| Legume N fixation | 25 kg N per t legume DM | Published NZ range 20–28 kg N/t ([review](https://rsnz.onlinelibrary.wiley.com/doi/full/10.1080/00288233.2023.2198719)) | test fixtures |
| NZ pasture production, dairy | ~14 t DM/ha/yr, over 20 irrigated | [Chapman et al. 2025](https://rsnz.onlinelibrary.wiley.com/doi/full/10.1080/00288233.2024.2425071) | T3 validation target |
| Temperate grass cool limit | ~4.4 C | same review | test fixtures |
| Measured seasonal growth, Lincoln P21 Low N (154 kg N/ha) and Woodlands (0 kg N/ha) | monthly kg DM/ha/day | [DairyNZ South Island averages, 2020](https://www.dairynz.co.nz/media/oxdi5vou/average-pasture-growth-data-south-island-2020-v1.pdf) | `data/calibration/`, T3 gate |

The two FAO-56 worked examples for extraterrestrial radiation and daylight hours
(Examples 8 and 9, 3 September at 20°S: Ra = 32.2 MJ m⁻² day⁻¹, N = 11.7 h) are
asserted directly in `tests/unit/SolarTest.cpp`, so the implementation is checked
against the source rather than against itself.

The example definitions under `data/` (`weather/`, `soils/`, `pastures/`) are
**PLACEHOLDER throughout** and marked as such in the files themselves. They
exist so the format has worked examples and so a schema change fails a test;
they are not a description of any real site, soil or sward.

## Terrain

| Parameter | Value | Source | Used in |
|---|---|---|---|
| Slope and aspect from a DEM | Horn's 3×3 finite difference | [Horn (1981), Proc. IEEE 69(1):14–47](https://doi.org/10.1109/PROC.1981.11918); the default in `gdaldem` and ArcGIS | `core/Topography.cpp` |
| Radiation on a slope | Numerical integration of the surface-normal / sun dot product | Method of [Allen, Trezza & Tasumi (2006), Ag & Forest Met 139:55–73](https://doi.org/10.1016/j.agrformet.2006.05.012), integrated numerically rather than analytically | `core/Solar.cpp` |
| Hill country slope classes | easy 15–20°, steep 25–30° | [Gillingham, Gray & Smith (1998), Proc. NZ Grassland Association 60](https://www.nzgajournal.org.nz/index.php/ProNZGA/article/view/2319) | `tests/unit/SolarSlopeTest.cpp` |

**A field check on the slope radiation model.** Gillingham et al. measured
pasture growth on a southern Hawke's Bay hill farm across north and south
aspects at both slope classes, and report that "pasture growth on north-facing
steep slopes in winter was greater than on any areas within the south aspect at
this time" — while soil moisture was *higher* on the south aspect throughout.
The north aspect therefore won its winter advantage despite being drier, which
leaves radiation to explain it.

`SolarSlopeTest.MidwinterRadiationRanksHawkesBayAspectsAsGillinghamMeasuredThem`
checks that the model ranks those four classes the same way. It is validation
rather than a pin: the ordering comes from a field trial by other authors.

It also records where the model and the measurement *disagree*. On the north
aspect, radiation alone favours the steeper face, because a steeper slope meets
the low winter sun more squarely; Gillingham still measured easy above steep
there. Light is not the whole story on the sunny side — soil depth and moisture
are — and the test says so rather than hiding it. Their numeric growth rates
are in bar charts, not tables, so only the ordering could be extracted.

## Example farms

The farm set is open: `data/farms/` is scanned, not enumerated, so what is
recorded here is the provenance of each file's numbers rather than a list the
code depends on.

| Parameter | Value | Source | Used in |
|---|---|---|---|
| Massey Dairy 4 effective area | 221 ha | [Massey University, Dairy 4 farm page](https://www.massey.ac.nz/about/massey-subsidiaries-and-commercial-ventures/massey-farms/dairy-farm-4/), "Effective Area: 221 hectares", retrieved 18 August 2026 | `data/farms/massey-dairy-4.toml`, asserted in `tests/config/DataFilesTest.cpp` |
| Massey Dairy 4 subdivision | approximately 80 paddocks of 1.5–3.5 ha | same page: "approximately 80 x 1.5-3.5 hectare paddocks all with race access" | same; also the source of the 2.5 ha default in `SyntheticParcelSource` |
| Massey Dairy 4 soils | Tokomaru silt loam, some Ohakea silt loam on lower terraces | same page | not yet used — this is what the S-map lookup should return for this farm |
| Farm centre coordinates, all three farms | NZTM2000 | LINZ Concord conversions, via the control point table in `tests/gis/ProjectionTest.cpp` | `data/farms/*.toml`, all marked `location_verified = false` |

**On the farm centres.** Each of the three files takes its coordinates from a
LINZ-converted control point for the *locality* - Ruakura, the Canterbury
Plains, Palmerston North - and none of them is a surveyed farm centroid. They
are right to within a few kilometres, which is right for solar geometry and
wrong for a fence. Every file says so in its `source` field and carries
`location_verified = false`; see open item 8.

**On the Massey extent.** The rectangle is not the farm's outline and is not
claimed to be. What is claimed, and tested, is that it encloses the published
221 ha and subdivides into 80 paddocks averaging 2.7625 ha - the published
count and the published mean. The generator's target size of 2.5 ha is tuned to
achieve that and is labelled as tuned in the file, because feeding in the
published mean instead yields 70 paddocks of 3.16 ha, which matches neither.

## Grazing systems (task #25)

The systems worth comparing are not a design choice: New Zealand hill country
extension literature describes three, and names what separates them.

> Smith, M E and Dawson, A D (1976). *Hill country grazing management.*
> Proceedings of the New Zealand Grassland Association, volume 38. MAF Advisory
> Services Division, Hamilton.
> [PDF](https://www.nzgajournal.org.nz/index.php/ProNZGA/article/view/1469)
> — running heads in the PDF put it at pages 47–54; the exact range is not
> stated on the article page, so it is not cited here.

| System | Definition, from the paper | Expected effect |
|---|---|---|
| Set stocking | "animals graze the pasture almost continuously"; spelling happens but "the process is uncontrolled and often highly selective" | Erect grasses and clovers suffer, browntop persists; "lower production and utilization" |
| The "shuffle" | An attempt at rotation with too few paddocks: "with a limited number of paddocks and five or six mobs, the effect must be to lengthen the grazing period or shorten the spelling period" | "little improvement on set-stocking" |
| True rotational | "the pasture is grazed for a short time and spelled for a long time" | Erect grasses and palatable legumes encouraged; more leaf area in winter, deeper roots in summer |

**Quantified guidelines, same source.** Graze no more than **3 days** with the
major mob. Minimum spell for the Hamilton region: **spring 12 days, summer 35,
autumn 35, winter 35**; summer may fall to 25 days "only if substantial rain
falls", and colder regions need "40 days plus" in winter. Context: hill country
farms then had **15–25 major grazing paddocks and 5–6 mobs**.

**The shuffle should emerge, not be implemented.** The paper defines it as what
happens when rotational intent meets an under-subdivided farm — too few
paddocks for too many mobs forces either longer grazings or shorter spells. A
model that carries paddock count, mob count and the two rotational parameters
will produce it on its own. Coding it as a third mode would be modelling the
symptom instead of the cause, and would lose the thing a farmer actually wants
to know: how many paddocks their farm needs before rotation starts paying.

### Validation target

Table 1 of the same paper, a Tauranga property running both systems at the same
stocking rate of 15.6 SU/ha:

| Hogget measure | Rotational | Set stocking | Difference |
|---|---|---|---|
| Liveweight, November (kg) | 45.5 | 38.5 | +7.0 |
| Liveweight, February (kg) | 50.5 | 44.6 | +5.9 |
| Fleece weight, March–October (kg) | 2.28 | 1.73 | +0.55 (+32%) |

The paper adds that this weight difference at mating implies "about a 12%
advantage in lambing percentage".

**How much this target is worth.** Less than the Gillingham and Frater ones, and
the difference should not be quietly forgotten. This is a 1976 advisory paper
arguing for a system its authors recommend; Table 1 is one property, not a
replicated trial, and the February figure is confounded because both groups were
rotationally grazed from October. It is a plausible magnitude to reproduce — a
few kilograms of hogget liveweight, not tens — rather than a number to hit. A
model that showed set stocking ahead, or showed a 30 kg gap, would be wrong;
one that shows 4 kg is not.

## Livestock energy (task #23)

The equations below are quoted from one document, cited here once and referred
to as **TMC** in the tables:

> Wheeler, D M (2018). *Animal metabolisable energy requirements.* Technical
> Manual for the description of the OVERSEER® Nutrient Budgets engine, version
> 6.3.0. AgResearch Ltd for OVERSEER Limited, June 2018. ISSN 2253-461X.
> [PDF](https://assets.ctfassets.net/bo1h2c9cbxaf/7eDPxk2KcUN3oMpe25Xd76/8e0b6c4b1c338e4301fc0ed38fda6d0f/TMC_Animal_Metabolisable_Energy_Requirements_v6.3_June_2018.pdf)
> — SHA-256 `f83171454056ecdd9e8e801e3958b9cbc387109c195dbebf0b34ebee7ffa016a`,
> 628 523 bytes, retrieved 18 August 2026.

The hash is here for the same reason the snapshot scripts record one: an
equation number is only traceable if the document it points into is the one that
was read. The PDF itself is not committed — see the licence note at the end of
this section.

TMC is a secondary source that states its own primary ones, and both are given
below. Where the primaries disagree, the disagreement is recorded rather than
resolved.

### Maintenance

| Quantity | Value | Source | Used in |
|---|---|---|---|
| Gross energy of feed | 18.4 MJ kg⁻¹ DM | CSIRO (2007), TMC Eq. 2 | `DietQuality::energy_density()` |
| Energy density qm | dietME / GE | CSIRO (2007), TMC Eq. 2 | `DietQuality::energy_density()`, and both efficiencies below |
| Efficiency of ME for maintenance km | **0.85 on milk diets; 0.35 qm + 0.503 otherwise** | CSIRO (2007), TMC Eq. 5 and 6. CSIRO's other published form, km = 0.02 (MJ ME/kg DM) + 0.5, is what Frater et al. quote | `DietQuality::maintenance_efficiency()` |
| Efficiency of ME for gain kgf | 0.035 × dietME × flegume × ftime (temperate pasture); 0.042 × dietME + 0.006 (tropical) | CSIRO (2007), TMC Eq. 9 and 11 | `DietQuality::gain_efficiency()` |
| Basal net energy | 0.28 × K × S × M × AgeFactor × lwt^0.75 MJ day⁻¹ | **Nicol & Brookes (2007), equation 1**, restated as TMC Eq. 13 | `basal_net_energy_mj()` |
| Species factor K | sheep 1.0, dairy 1.4, dairy replacements 1.4, beef 1.4, deer 1.7, dairy goats 1.25 | TMC Eq. 13, following CSIRO (2007). **Nicol & Brookes use 1.0 sheep, 1.3 beef, 1.5 dairy, 1.4 deer** — see below | `basal_net_energy_mj()` |
| Sex factor S | 1.15 entire males; 1.075 mixed-sex mobs; 1.0 females and castrated males | TMC Eq. 14 | `basal_net_energy_mj()` |
| Milk factor M | 1 after weaning; 1 + (0.26 − Mage × age/7) before, Mage = 0.26 / weeks suckled (0.015 dairy and beef, 0.010 otherwise) | SCA (1994), TMC Eq. 15–16. **Nicol & Brookes did not include M** | `milk_factor()`, into `basal_net_energy_mj()` |
| Age factor | exp(−0.00008 a), a in days, floor 0.84 | Freer et al. (2006), TMC Eq. 17. Frater et al. quote exp(−0.03 A) with A in years; TMC records the two methods as differing by about 0.14% on average | `age_factor()`, into `basal_net_energy_mj()` |

**On K.** The two primaries genuinely disagree, and which one is picked changes
maintenance for beef cattle by about 8%. TMC §4.1 follows CSIRO for sheep, dairy
and beef and Nicol & Brookes for deer. This project has no basis yet for
preferring either, so the choice belongs in a data file with both values
recorded, not hard-coded.

**An independent check on the 0.28.** TMC notes that Simpson (1978b) reported
values for maintenance ME per kg lwt^0.75 of 0.40 for sheep and 0.55 for cattle.
Take a pasture diet of 10.5 MJ ME/kg DM: qm = 10.5 / 18.4 = 0.571, so Eq. 6
gives km = 0.35 × 0.571 + 0.503 = 0.703, and the net 0.28 becomes
0.28 / 0.703 = **0.40 MJ ME/kg lwt^0.75** — Simpson's sheep figure, from a
different author two decades earlier, with no free parameter in between. That
is a validation target rather than a regression pin, and it is worth asserting
when the code exists.

The arithmetic also settles which way round Eq. 5 and 6 go: the 0.85 belongs to
milk diets and the qm expression to everything else. Reading them the other way
gives km = 0.85 on pasture and a maintenance requirement of 0.33, which matches
nothing published.

### Grazing, and why it depends on the terrain

These are the equations that connect livestock to the slope and aspect work
already merged (#19, #20), which is why they matter beyond bookkeeping.

| Quantity | Value | Source |
|---|---|---|
| Chewing | NEchew = lwt × SpGraze × DMintake × (0.9 − digest/100) | TMC Eq. 18 |
| Dry matter intake | DMintake = MEintake / dietME | TMC Eq. 19 |
| Grazing coefficient SpGraze | 0.0025 for dairy and beef | TMC Eq. 20 |
| Movement | NEmove = 0.0026 × lwt × SlopeMoveFactor × (TSR / SD) / fmove | TMC Eq. 21 |
| Pasture-mass term | fmove = 0.000057 × PastureMass × 1000 + 0.16 | TMC Eq. 22 |
| **Slope term** | SlopeMoveFactor = 1 + tan(slope) | TMC Eq. 23 |
| Activity | NEactivity = lwt × (0.0026 × Hkm + 0.028 × Vkm) | TMC Eq. 24 |

`SlopeMoveFactor = 1 + tan(slope)` is the whole link: it is 1.00 on the flat,
1.18 at 10°, 1.36 at 20° and 1.58 at 30°. The vertical component of Eq. 24 costs
about eleven times the horizontal one per kilometre. A farmlet on a steep face
therefore pays twice over: the pasture on it grows against a slope radiation
ratio from `SlopeRadiationTable`, and the stock on it spend more energy walking.

### Liveweight change

| Quantity | Value | Source |
|---|---|---|
| Net energy of liveweight change | NElwt = lwtchange × 0.92 × EVG (0.92 = liveweight to empty body weight) | TMC Eq. 43 |
| Energy value of gain | EVG = (6.7 + R) + (k1 − R) / (1 + exp(−6 (Z − 0.4))), k1 = 16.5 large lean cattle breeds, 20.3 otherwise | TMC Eq. 44 |
| Maturity | Z = lwt / SRW | TMC Eq. 45 |
| Rate adjustment | R = (lwtchange × 1000 × 0.92) / (4 SRW^0.75) − 1 | SCA (1994), used by Nicol & Brookes (2007), TMC Eq. 46 |

This is the gap the previous revision of this file recorded as open. It is now
closed from a primary-stating source, so the model no longer has to
reverse-engineer a gain cost from a single worked example.

Evaluating it as a sanity check: at R ≈ 0, EVG is about 11.4 MJ/kg empty body
gain for a young animal (Z = 0.2) and about 26.5 MJ/kg near mature weight
(Z = 1.0). Dividing by an efficiency of gain of 0.4–0.5 puts ME between roughly
21 and 60 MJ per kg of liveweight gain. A figure of "25–55 MJ ME/kg" is widely
attributed to Nicol & Brookes in secondary summaries; that range is consistent
with these equations, but **it is not cited here**, because it was found in a
search result rather than read in a document.

### Validation target

Frater, Howarth & McEwen,
[J. NZ Grasslands](https://www.nzgajournal.org.nz/index.php/JoNZG/article/download/477/105/2053),
Table 1, for a weaned lamb of 28 kg gaining 100 g/day at 100 days old on a
10.5 MJ ME/kg DM diet, with grazing costed at maintenance + 15%:

| Model | MJ ME/day | Intake (kg DM/day) |
|---|---|---|
| Nicol & Brookes (2007) | 9.72 | 0.93 |
| CSIRO (2007) | 9.71 | 0.92 |
| OVERSEER | 8.77 | 0.83 |

Every term needed to reproduce 9.72 is now in the tables above, so this is a
test to write against the implementation rather than an open literature item.
The three models differing by 10% on the same animal is itself the useful fact:
it sets the accuracy this part of the model can honestly claim.

### What is implemented, and what is not

`core/AnimalEnergy.cpp` implements the maintenance chain (TMC Eq. 2, 5–6, 9,
13, 14, 17), the grazing terms (Eq. 18, 20–24), liveweight change (Eq. 43–46,
81) and the iterative solution of the chewing circularity (Eq. 52, 54–55).

The inverse - what a given intake does to an animal's weight - is also
implemented, and it is the direction a simulation needs: the manual answers
"to grow this fast, what must it eat", and a farm needs "having eaten this, how
fast does it grow". The efficiency is asymmetric and the asymmetry is the
manual's: TMC Eq. 8 gives km / 0.8 for a non-lactating animal losing weight
against kgf for one gaining, so mobilising tissue is more efficient than
depositing it and the same energy gap moves the animal further down than up.

Two readings there are this project's rather than the manual's, both flagged in
the code. TMC Eq. 54 charges a tenth of the production cost to maintenance;
that share is not charged on a deficit here, because an animal losing weight is
not producing and the manual does not cover the case. And **starvation is not
modelled**: an underfed animal loses weight indefinitely, with no floor and no
mortality, so a run that reaches implausible weights is reporting a feed budget
that does not work rather than an animal that survived it.

**Not implemented, and a farm model is incomplete without them:**

| Missing | Equations | Consequence |
|---|---|---|
| Lactation | TMC Eq. 33–35 | **Modelled for sheep**, on TMC Eq. 33, 35, 36, 38 and 46: milk yield from days lactating and the number on the ewe, net energy per kilogram from its fat and protein, charged through Eq. 54's production tenth. A **dairy cow** is still a dry cow - the equations are general and no dairy class has been given the parameters or checked against a milking one |
| Pregnancy | TMC Eq. 26–32 | No cost of carrying a calf or lamb, so late-gestation demand is absent |
| Wool and velvet | Eq. 7, §4.8–4.9 | Sheep and deer are cheaper to keep than they are |
| Pre-weaned animals | §5.2.4, Eq. 15–16 | The milk factor M is fixed at 1, which is only correct after weaning |
| Cold, heat, wet and wind | §4.10 | No weather cost on the animal, only on the pasture |

The seasonal temperate-pasture form of the efficiency of gain (Eq. 11, with
`flegume` and `ftime`) is also not used. TMC states it and then says it "has not
been implemented" in OVERSEER either; separately, the text of its `ftime` term
did not survive PDF extraction unambiguously — it reads
`ftime = 1 + 0.12 8 (lat * sin(0.0172 day)/40))`, with an unmatched bracket and
a space inside what is probably 0.128. Eq. 9 is used instead, which is
unambiguous and is what OVERSEER runs.

One reading is this project's and not the manual's: TMC gives km for milk diets
(Eq. 5) and for everything else (Eq. 6) but not for a diet that is part milk.
`DietQuality::maintenance_efficiency` blends the two by the milk fraction. That
is the obvious interpolation, and it is flagged here rather than presented as
sourced.

### Grazing: what the coupling does and does not do

`core/Grazing.cpp` takes a mob's intake from the energy chain above and removes
it from the sward, capped at what stands above each species' residual. Two
simplifications are large enough that a reader should meet them here rather
than discover them in a long run.

**Dung and urine are returned, and what stands behind them is thin.** This
paragraph used to say the opposite - that nitrogen eaten left the system and
never came back, so a long run stripped the farm. It comes back now: dung at
TMC Eq. 137's rate, wool and liveweight gain at placeholder rates, and urine as
the remainder, landing in patches at OVERSEER's stated loading rather than
spread evenly over the paddock. Leaching follows in two pools, under the patch
and between patches.

The honest reading of that is not "solved". It is that a farm which used to be
missing its largest nitrogen pathway now has one resting on two placeholders and
one fitted fraction, and a leaching figure from it is an order of magnitude
rather than a measurement. See the nitrogen model document for the table of
which is which.

**Grazing is not selective.** Grass and legume are taken in proportion to what
each offers above its residual. Smith and Dawson (1976) report that set stocking
is "highly selective" and that clovers under it "tend to be overgrazed, existing
only as stunted plants" - so the species-composition half of their finding is
outside what this model can currently show. Quantifying a preference needs a
source this project does not yet have; see open item 10.

### Two errors a year-long run caught

Both were found by running `data/scenarios/canterbury-grazed` for a year, not by
any unit test, and both are recorded because the class of mistake matters more
than the fix.

**Demand must come from what an animal wants, not what it did.**
`daily_energy_requirement` answers "to change weight at this rate, what must it
eat", so feeding it a *realised* rate inverts the question. A mob that lost
weight overnight returned a negative production term, which reads as an energy
credit and shrank the requirement - so it ate less, lost more, and asked for
less again. A year took a mob from 55 kg to two grams on a farm whose mean cover
never fell below 2000 kg DM/ha and which reported feed-limited on two days out
of 366. Demand is now computed against holding weight. Appetite rising when an
animal goes short is real and is not modelled; that needs intake capacity, which
has its own literature.

**Set stocking is not "do not move".** Modelled as leaving a mob where it
stands, it confines the stock to one paddock - the opposite of the system. Smith
and Dawson (1976) say of lambing that "the whole of the farm area should be used
for grazing". The mob lost fifteen kilograms over seventy days on two hectares
while forty-seven other paddocks carried feed. A mob now holds a *set* of
paddocks: one under rotation, all of them under set stocking. That also makes
the agronomic point fall out on its own - under set stocking every paddock is
grazed every day, so none of them rests, which is why the source says the system
grows less.

### Calibrating against New Zealand industry sources

Six sources were fetched and read before anything was recorded against them, and
`data/calibration/livestock/sources.toml` holds the manifest with hashes. Three
findings came out of doing that rather than taking the numbers on trust.

**Cattle maintenance validates to 2%.** DairyNZ's *Lactating cow* page gives
maintenance ME by liveweight, stated as calculated at 11.0 MJ ME/kg DM. The
equations already in `core/AnimalEnergy.cpp` reproduce it across the whole
published range:

| Liveweight | DairyNZ | This model | Difference |
|---|---|---|---|
| 300 kg | 40 | 39.7 | −0.8% |
| 400 kg | 50 | 49.2 | −1.5% |
| 500 kg | 59 | 58.2 | −1.4% |
| 600 kg | 68 | 66.7 | −1.9% |

This is the cattle validation this file had been missing, and it is stronger
than the Simpson sheep check because it spans a range rather than a point.

**But only with the age factor set to one**, and that question is now answered:
the age term does apply to adult cattle. The OVERSEER ME review works an example
for a 500 kg animal at four years using it —
`MEm = ((0.36 W^0.75)/km) * exp(-0.00008 A)` — and discusses its effect for
animals of six years and more. So the 17% between the model and DairyNZ at
500 kg is a difference between a mechanistic framework and a practical table,
not evidence that the age term belongs only to growing stock.

**The floor is a different matter, and the two OVERSEER documents contradict
each other about it.** The technical manual states "Agefactor had a minimum
value of 0.84 (Freer et al., 2006)". The review asks why "the lower bound on
AgeFactor was not used as in the Freer et al. (2010) expression", notes that
CSIRO (2007) and Nicol and Brookes (2007) place no lower bound, and says the
omission "may allow ME requirements to drop too low for animals older than 6
years".

This implementation has the floor, which matches what the manual documents and
what the review argues for. It should not be described as what the reviewed
OVERSEER code does, because the review says that code lacks it. Open item 11 is
now about which variant to carry, not about whether the term applies.

**Sheep: the earlier 26% was a comparison against the wrong table.**

Beef + Lamb NZ publish two sheep maintenance tables, from two frameworks, in two
documents, and carry the same worked example in the same words with different
answers:

| Publication | Framework | 50 kg ewe at maintenance |
|---|---|---|
| A guide to feed planning for sheep farmers, Appendix 1.2 | Nicol and Brookes (2007) | 8.0 MJ ME/day |
| Making every mating count, Appendix 3.1 and Table 2.2 | Geenty and Rattray (1987) | 10.0 MJ ME/day |

The equations here descend from Nicol and Brookes by way of the OVERSEER manual,
so the first is the comparator. Against it the model is 1.9% low at 45 kg, not
26%.

**But the agreement is not clean, and what is left is more interesting than
either number.** The deviation grows with weight:

| Ewe | Nicol and Brookes | This model | Difference |
|---|---|---|---|
| 45 kg | 7.0 | 6.87 | −1.9% |
| 50 kg | 8.0 | 7.43 | −7.1% |
| 60 kg | 10.0 | 8.52 | −14.8% |
| 70 kg | 11.0 | 9.56 | −13.0% |

An earlier note here read an exponent out of that and said the table was steeper
in liveweight than W^0.75. **That was over-reading a rounded table.** Its steps
are 1.0, 1.0, 1.0, 0.5, 0.5 MJ - rounded to the half-megajoule, with a clear
change of slope at 60 kg. Fitting a power law gives 1.24 over 45-60 kg, 0.62
over 60-70, and 1.02 overall, which is three different answers from six rounded
points and means none of them.

What is left is a better question. The ME review quotes the Nicol and Brookes
maintenance equation in full:

    MEm = K.S.M (0.28 W^0.75 exp(-0.03A))/km + 0.1 MEp + MEgraze + Ecold

It carries **MEgraze** and **Ecold**, and this implementation has neither in its
maintenance figure - grazing cost is computed separately and cold is not
modelled at all. A published table of requirements for *grazing* ewes would
include them, and grazing cost scales with liveweight rather than with W^0.75,
which would put a model without it increasingly below the table as the animal
gets heavier. That matches the direction of what is seen. It is a hypothesis
with an equation behind it rather than a measurement, and testing it needs the
chapter itself.

The Geenty and Rattray table stays, as a separate framework rather than an
error. What its practical grazing allowance is made of needs the 1987 chapter,
which has not been read.

**One attribution in the brief was wrong, and it mattered.** The OVERSEER ME
review was cited for K = 1.4 for beef cattle. The review does quote OVERSEER's
values — 1.4, 1.4, 1.0, 1.4, 1.7, 1.25 — but its Recommendation 6 is to *change*
them: "Modify the value of Kantype in the BASAL equation to be 1.0 for sheep,
1.3 for British cattle breeds and 1.5 for beef and dairy cattle of dairy
origin." It also notes that OVERSEER's equation "is cited as derived from Nicol
and Brookes (2007), but uses the K values" of another source. So the K table is
a record of what OVERSEER does, not a value that review endorses — and the
disagreement recorded above now has an independent reviewer on the 1.3 side.

The same review gives a route for the cold-stress term this model lacks
(Recommendation 9): CSIRO (2007) equations, NIWA monthly climate, and Cottle and
Pacheco (2016) for fleece depth.

### What is still not sourced

- **Nicol & Brookes (2007)** itself — NZ Society of Animal Production Occasional
  Publication No. 14, pp. 151–172 — has not been read. Every attribution to it
  above is TMC's, and is marked as such. It is not freely available online.
- **The Massey thesis** listed in an earlier revision is unreachable from this
  environment: `mro.massey.ac.nz` refuses the TLS handshake, and curl,
  PowerShell and an HTTP fetch all failed at the network layer rather than on
  content. It is not cited above.
- **Standard reference weight (SRW)** by breed and class, which Eq. 45 and 46
  both need, is in a different TMC chapter that has not been retrieved.

### Licence note

TMC's copyright statement permits copying and use of the report and the
information in it, on condition that the use does not mislead as to its
contents, that any copy carries the disclaimer in full, and that neither the
report nor its contents are used "in connection with any promotion, sales or
marketing of any goods or services". Equations are facts and are not restricted
by copyright in any case, but the last clause is a real constraint on a project
that may become commercial, and it belongs with the Open-Meteo and VCSN terms in
open item 7 rather than being discovered later. The PDF is therefore not
committed; the URL and hash above are enough to retrieve the same document.

## What each numeric assertion in the tests actually rests on

Not every number in a test is evidence of the same kind, and treating them alike
is how a project convinces itself it has validated something it has only pinned.
The suites here fall into three kinds, and the comments say which:

- **Validation** - compared against a value published by someone else. The
  NZTM control points come from LINZ's own conversion service; the false easting
  of 1 600 000 m is LINZ's definition; the level-ground radiation case is FAO-56
  Eq. 21.
- **Verification** - checked against arithmetic a reader can repeat. Slope and
  aspect on a tilted plane are closed-form; a 200 m by 200 m block at 10 m cells
  is 400 cells; the equinox equivalent-latitude identity follows from solar
  geometry and holds whatever this code does.
- **Regression pins** - numbers that came out of this implementation, kept so
  that a change has to be deliberate. They are labelled as pins in the tests
  that use them, and they are **not** evidence that the value is right. The
  midwinter slope ratios of about 2.00 and 0.06, the 0.9 floor on a midsummer
  tilt, and the 0.1 ha bound on rasterisation error are all of this kind.

## What this model may be quoted for

The evidence is uneven, so this says plainly which outputs carry weight today
and which do not. It is the short version; the sections above are the working.

| Output | Standing | Why |
|---|---|---|
| Conservation of dry matter, water and nitrogen | **Sound** | A property of the bookkeeping, not of any parameter. Closes to 1e-9 over a grazed year |
| Deterministic replay | **Sound** | Same reason |
| Comparison between grazing systems | **Sound for direction; treat the size with care** | Both arms carry the same stock, the same parameters and the same structure, which is what makes the comparison worth more than either figure alone. It does not make the difference exact: the responses are non-linear, so a wrong parameter can bias the magnitude - by more in one arm than the other, because the arms sit at different points on the same curve |
| Direction of a seasonal or terrain effect | **Sound** | Checked against Gillingham's field trial for slope and aspect |
| Cattle maintenance requirement | **Sound to about 2%** | Reproduces DairyNZ across 300-600 kg |
| Sheep maintenance requirement | **Low by about 5%** | Against CSIRO (2007) at Adjabui et al.'s own 60 kg ewe, and 12% against Nicol and Brookes (2017). The **basal term agrees to 0.3%** - it is the same equation - and the remaining gap is the cost of grazing, walking and activity, which both published figures include. **This was 15% and 21% until the walking distance was supplied** (E10): TMC Eq. 24 was implemented and fed by nothing, so the model charged about a tenth of the published grazing cost and now charges about seven tenths of it. **CSIRO is the comparator, and the choice is not the flattering one being picked**: Adjabui et al. record that CSIRO accounts for chewing and ruminating inside km, while Nicol and Brookes add them separately on top of the same km, and that AgResearch (2016) asked the authors to correct that double count. This model uses km the CSIRO way, so measuring against the other figure would book somebody else's arithmetic as this project's error. Both are reported. The earlier 2% to 15% arrived at nearly the right number by the wrong route - a bare basal term subtracted from a grazing-inclusive published one. See `SheepMaintenanceAgainstAdjabuiWithGrazingIncluded` |
| **Carrying capacity for sheep** | **Overstated, by about 5%** | Follows directly from the line above: a 60 kg ewe on steep hill is modelled as needing 8.6 MJ ME/d where CSIRO implies 9.0, so a farm still looks able to carry more stock than it can - but by a twentieth rather than the sixth it was before the walking was charged |
| **Absolute liveweight gain** | **Not quotable** | Standard reference weight is `verify` on every species definition, and it drives the energy value of gain |
| Anything involving a milking cow | **Not modelled** | Lactation is absent; a "dairy cow" here is a dry cow |
| Nitrogen over more than a season | **An order of magnitude** | Dung and urine are returned and leaching is modelled, but patch uptake is a placeholder and the inter-patch share is fitted |

The sheep figure is the one to watch, and it is an **approximation carried
deliberately** rather than an unknown: the size and sign are measured, a test
holds them inside 2 percentage points of the figures above, and open item 12
names the chapter that would settle the rest. It narrowed from 15% to 5% when
the walking distance was supplied - the same test that pinned the old gap
pinned the new one, so the improvement is measured rather than asserted.
Work that compares scenarios is unaffected. Work that answers "how many ewes
will this farm carry" is closer than it was and still should not be published
from this model until item 12 closes.

## Open items

These are gaps in *evidence*. What is scheduled to be *built*, and in what
order, is in [backlog.md](../backlog.md) - which also marks which of these block
work and which do not.

| # | Item | Needed by | Source to check | Status |
|---|---|---|---|---|
| 1 | Pasture growth: base and optimal temperatures, seasonal growth rates, annual DM yield by region | M2 | DairyNZ, AgResearch | open |
| 2 | Soil water: profile available water (theta_FC, theta_WP, rooting depth) by S-map soil class, drainage class | M2 | Manaaki Whenua S-map | **partly routed.** Manaaki Whenua publishes profile available water, potential rooting depth and drainage class - through S-map where it has coverage, and the older Fundamental Soil Layers as a fallback it is replacing. What is *not* published per soil, as far as located, is separate volumetric field capacity and wilting point. That may not matter: this model's bucket takes total available water through the profile, which is what PAW already is, so splitting PAW into FC and WP to satisfy the shape of the ask would be inventing two numbers to reconstruct one. Two conditions on using any of it: S-map is nominally 1:50,000 and is not a site measurement, so a paddock figure must be labelled as a mapped estimate; and its licence forbids redistribution - see item 7 |
| 3 | Intake, stocking rates, stock-unit conversions | M3 | Beef+Lamb NZ, DairyNZ | energy equations sourced (see Livestock energy); standard reference weight by breed and class still needed |
| 16 | **Flock turnover: replacement, mortality and culling.** Two published sources disagree about replacement and both are carried rather than one being quietly chosen. OVERSEER's *Characteristics of animals* (Table 29) gives sheep 20%. Ridler, Corner-Thomas, Mote, Morgan, Kenyon and Flay (2025), *New Zealand Veterinary Journal* 73:112-123, measured 34 New Zealand flocks and report a between-flock mean replacement of **29.2% (SD 5.0)**, with **10.5% (SD 4.6)** of ewes presented for breeding culled or dead by mid-lactation, **two-thirds of those deaths during lambing**, and **16.5% (SD 8.3)** of the ewes present at weaning culled then - most often for age, incisor teeth defects and udder defects. The field study is the default here because it is recent and directly measured; the OVERSEER figure is a model parameter. **Still open**: the age at which a ewe is culled. The study names age as the commonest reason and does not give it, and a 29.2% replacement implies an average retention near three and a half years rather than a hard limit, so `cull_age_years` is a PLACEHOLDER | M5 |
| 4 | Facial eczema: spore-count thresholds and warm-wet trigger conditions | M4 | DairyNZ | **answered for the trigger and the thresholds; the production loss is still open.** All quoted from DairyNZ. **The weather trigger**, from the *Technical Series* (February 2012): conditions favour sporulation "when overnight minimum grass temperatures stay at or above 12C over four consecutive nights, and humidity is high e.g., with drizzly rain (4-6 mm/48 h), or when soil is kept moist by irrigation". January to May are the high-risk months, and outbreaks typically show "one or two small increases over several weeks, followed by a major rapid rise" - so the shape is a slow build then a spike, not a ramp. **The thresholds**, from *Facial Eczema - Management for New Zealand dairy herds*: 20,000 spores/g pasture is where regional counts trigger monitoring your own farm (and the ceiling above which fungicide is not applied), 30,000 is where a full zinc dose starts, and a programme stops only when farm counts are "consistently at 10,000 or less for three weeks" with cooler temperatures. Counting is noisy in a way a model should not pretend away: the same guide puts the test's variability at +/-10,000 spores/g between 0 and 50,000, rising to +/-45,000 above that, and paddocks on one farm can differ by 500,000. **The epidemiology**: "Only about 10% of affected animals show clinical signs, for every clinical case there will be 10 cows with sub-clinical FE", GGT "levels of 250 IU/L indicate moderate damage", and young spores carry more toxin so fresh sporulation is worse than an old standing count. **The production response, for dairy, is sourced and linear.** Cuttance, Mason and Laven (2021), *New Zealand Veterinary Journal* 69(4):201-210, blood-tested whole herds against individual milk-solid production and report that "an increase of 100 IU/L in GGT activity was associated with a decrease of 0.011 (95% CI = 0.010-0.012) kg MS/cow/day", with a GGT threshold of 40 IU/L giving the largest herd-level association at 6.14 kg MS/day/100 cows. Two things in the same paper keep a model honest about scale: the prevalence of cows over 40 IU/L ranged from 11% to 96% **between farms**, and individual GGT ran from 3 to 6001 IU/L. **Still open, and it is the half this project needs most**: no equivalent quantified response was found for sheep. The literature describes reduced daily gain, wool loss and lowered reproduction without numbers, and every scenario shipped here runs ewes - so the dairy curve is sourced and the sheep curve is not, and must not be borrowed from it. **For sheep the liver end is sourced even though the production end is not.** Morris, Smith and Hickey (2002), *New Zealand Veterinary Journal* 50(1):14-18, dosed Romney lambs with sporidesmin and regressed liver injury score on serum GGT: LIS = -2.96 (SE 0.38) + 0.89 (SE 0.07) x ln(GGT), R2 = 0.54, p<0.001, unchanged between FE-resistant and control sire lines. The New Zealand ram-breeding tolerance test is GGT measured 21 days after a measured sporidesmin challenge, with reactors taken as GGT above 55 IU/L. Experimental oral doses in the literature are 0.5, 1.0 and 3.0 mg sporidesmin/kg liveweight, 1.0 producing severe disease uniformly. Field guidance puts 100,000 spores/g as dangerous, notes 50,000 as dangerous over long grazing and 20,000 as damaging over an extended period, and puts clinical signs 10-18 days after intake. **The conversion between the two halves is sourced, and checking it is what settles how the middle must be modelled.** Fitzgerald, Collin and Towers (1998), *Letters in Applied Microbiology* 26(1):17-21, measured both quantities in the same New Zealand pasture trial: "maximum sporidesmin levels of 26 ng g-1 grass in treated pasture and 113 ng g-1 grass in untreated pasture ... when spore numbers had reached a maximum of 80,000 spores g-1 grass in the untreated plots and 50,000 spores g-1 grass in the treated plots". That is **1.41 pg sporidesmin per spore in the untreated pasture and 0.52 in the treated** - the same count carrying 2.7 times the toxin depending on which strains are present, so this is a central value with a documented reason to be wrong, not a constant of nature. The treated plots were seeded with atoxigenic strains, which is exactly the point of that trial.

**Working the chain through is what rules out the obvious model.** At 100,000 spores/g - the level field guidance calls dangerous - the untreated ratio gives 141 ng/g grass, and a 60 kg ewe eating 1.5 kg DM/day ingests 0.21 mg/day, or 0.0035 mg/kg liveweight/day. Accumulating the 1.0 mg/kg that produces severe disease as a single experimental dose would take **283 days at that rate, against the 10-18 days field guidance gives for clinical signs**. The gap is about sixteen-fold and does not close on its own. It is explainable - "g grass" may be fresh rather than dry weight, stock grazing low eat the dead litter at the sward base where spores concentrate rather than the paddock average, and counts run far above 100,000 - but every one of those is an unsourced assumption, and stacking them to force agreement is how a model acquires a number nobody can defend.

**So the acute single-dose figures must not be used as a cumulative threshold.** The dose to GGT step has to be calibrated empirically against the field spore-count thresholds instead, and labelled as an empirical index rather than presented as toxin mass balance. The two ends stay mechanistic and cited; the middle is fitted, and says so. **One more gap, recorded rather than corrected**: the trigger is stated on *grass* minimum temperature and this model carries only air temperature. A grass-level thermometer radiates to the sky and reads below the screen minimum - that is what a ground frost is - so grass minimum sits at or under air minimum and testing the air reading against a grass threshold **over-triggers rather than under-triggers**. It is least wrong where it matters: radiative cooling is what opens the gap and is suppressed under the damp overcast skies the rainfall condition selects for. Left uncorrected because no published conversion between the two has been located. Also unverified: a figure of about 10^5 spores/g dry pasture as outbreak level, seen only in secondary sources, and a 25% three-week herd production drop attributed to AgResearch trials on a commercial page with no citation |
| 5 | Grass grub: degree-day development model | M4 | AgResearch literature | open |
| 6 | Nitrogen leaching: regulatory thresholds | M4 | Current Regional Council rules | open |
| 7 | Licence and **redistribution** terms for every dataset and document in use | M2-M3 | Dataset and document licences | **narrowed, and it splits in two.** Citing a source and shipping it are different permissions, and this project had only been tracking the first. LINZ data is CC BY 4.0 with attribution and commercial use allowed, and the elevation collection states its own licence, so it may travel. Open-Meteo is CC BY 4.0, with the upstream model sometimes needing its own attribution. NIWA's DataHub licences **forbid passing the data to third parties**, and S-map Online is CC BY-NC-ND 3.0 NZ - non-commercial, no derivative distribution - so neither may be committed or shipped, whatever their hashes say. A correction to what this row used to assert: **no source has been found for an OVERSEER "no-promotion clause"**; the public Knowledge Base permits distributing a report in its entirety with its disclaimers intact, and the terms that matter are whatever the specific PDF in use carries. That claim should not have been written without one |

| 8 | Farm boundaries and centroids for the three example farms | M3 | LINZ NZ Primary Parcels, via `scripts/linz-snapshot.py` | **narrowed - the fetch works, the selection does not.** A real 6 km square of NZ Primary Parcels around Lincoln has been pulled from the LINZ Data Service (`layer-50772`, 5943 parcels, EPSG:2193 easting-first as returned, SHA-256 `b1ce4586d4b114506587b86bdda15981061a80ad021d46bc9063d172df2a3e1f`). Which of those parcels are the farm is open: see item 15. All three farms still ship with generated boundaries marked `location_verified = false`. Note this is now only the boundary: every shipped farm's *ground* is measured, from LINZ's open elevation, which needed no key and no parcel selection. The parcel snapshot did earn its keep here, though - it is what placed the demonstration block on farmland instead of on Christchurch |
| 9 | Where stock choose to graze on a paddock of varying slope: utilisation by slope class | M3, task #24 | Lambert and Gillingham on stock camps and nutrient transfer | open - Gillingham et al. (1998) gives pasture *production* by slope and aspect, but not the animals' *distribution* over it. The energy cost of walking a slope is sourced (TMC Eq. 23); the preference that follows from it is not |

| 10 | Grazing selectivity: how strongly stock prefer clover over grass, and how that differs between set stocking and rotation | M3, task #24 | NZ grazing behaviour literature | open - the direction is stated by Smith and Dawson (1976), the magnitude is not |

| ~~11~~ | ~~Which age-factor variant to carry~~ | M3 | — | **decided: carry the 0.84 floor.** It is what the manual documents and what the review argues for, so both sources agree on the outcome even while disagreeing about what the reviewed code does. Applicability to adult cattle was settled first: the review works a 500 kg four-year-old through the age term |
| 12 | Whether Nicol and Brookes' published ewe maintenance includes MEgraze and Ecold, which this model's maintenance figure does not | M3 | Nicol and Brookes (2007), *Pasture and supplements for grazing animals*, NZSAP Occasional Publication No. 14 - **listed on nzsap.org**, so obtainable | **answered for grazing, still open for cold.** Adjabui et al. (2025), *Livestock Science* 299:105766, write the Nicol and Brookes (2017) maintenance as the basal term over km **plus MEgraze, MEmove and MEactivity** (their Eq. 2), which settles the half of this question that was blocking a comparison: the published figure is grazing-inclusive and this model's was not, so the two were never like for like. The corrected gap is 21%, not 15% - see the caveat table above and `tests/validation/LivestockCalibrationTest.cpp`. **Cold is not addressed**: that paper never mentions cold stress, so whether Ecold is in their figure is still unknown. On the year: the paper's own reference list carries **both** a 2007 and a 2017 entry for the same chapter, same title, same pages 151-172, so both printings exist and recording both was right |
| 13 | What the Geenty and Rattray (1987) practical grazing allowance is made of | M3 | Geenty & Rattray, "The energy requirements of grazing sheep and cattle", NZSAP Occasional Publication No. 10, pp. 39-53 | **open, and the volume is confirmed to exist.** NZSAP's site hosts Occasional Publications 11 to 16 and not 10, but *Livestock Feeding on Pasture*, Occasional Publication No. 10 (1987, about 145 pp.) is catalogued with this chapter beginning at p. 39, and NZSAP's own later papers cite it as pp. 39-53. So it is not a phantom citation and not established as unobtainable - it is simply not openly digitised anywhere located so far. The route is a library or NZSAP directly rather than more searching. Until the text is in hand the 2 MJ/day it runs above Nicol and Brookes stays an **observed difference between two frameworks** and must not become a term: no `maintenance += 2.0`, no multiplier |
| 14 | Whether OVERSEER uses lwt^0.75 or lwt^0.73 | M4 | TMC Eq. 13 against the ME review's section 12 | open - the manual says 0.75 in six places; the review reports `W^0.73 (Wheeler 2016b, Appendix 2)`. This implementation follows the manual. Another manual-against-implementation difference, like the age factor floor |
| 15 | Which LINZ parcels are LURDF | M3 | An authoritative statement of the farm's location or boundary, from Lincoln University or a survey plan | **narrowed - a map exists.** LINZ's open cadastre carries no owner, so the parcel snapshot cannot say whose land a parcel is, and forty-seven parcels of over 15 ha are centred in the fetched square. Choosing one on area would produce a boundary that looks surveyed and is invented. What has changed is that a Lincoln University dissertation has been identified carrying **"Map of the LURDF and surrounding Lincoln University farms", Figure 4-1, p. 21** (Lincoln University Research Archive). That is a route: georeference the figure by hand, overlay the LINZ parcels, record the candidate parcel ids, check the merged survey area against 79 ha, and keep the geometry LINZ's rather than drawing it. Correction to an earlier note here: the university's own page is not inaccessible - automated retrieval returns 403, but it is indexable, and it gives 79 ha total, 72 ha under irrigation, 200 stock, Weedons Road |

Item 7 also gates the repository licence and what may be redistributed with a
release, so it is worth settling before M2 rather than at M5.

Item 8 is what turns the example farms from plausible into real, and the format
is already in place to receive it: a farm switches from generated rectangles to
LINZ parcels by changing its `[boundary]` section to `kind = "geopackage"`.

## Engineering caveats

Recorded here because they affect whether a result can be trusted, not because
they are parameters.

| # | Caveat | Milestone |
|---|---|---|
| ~~E1~~ | **Closed in M2.** `std::` distributions are implementation-defined, so the same seed gave different numbers on different standard libraries. Core now implements its own — see [ADR 0007](../adr/0007-own-distributions.md). Uniform deviates are bit-identical across platforms; normal, exponential and gamma agree to within four units in the last place, limited by libm's rounding of `log`, `sqrt` and `pow`. Golden vectors are asserted on all three CI platforms. | M2 |
| E2 | The conservation suite currently exercises the ledger, not agronomy. Every process added in M2 must declare which budget lines it touches and report its flows, or the gate silently proves less than it appears to. | M2 |
| E3 | Coordinate transforms have no round-trip test yet because `gis/` has no PROJ dependency yet. The 1 mm control-point requirement lands with the first transform. | M3 |
| E4 | The synthetic weather generator draws wet days independently, so it has no wet spells and cannot produce a realistic drought. Keeping each day keyed by its date is what makes any subrange of a run reproducible, and a Markov chain would need an arbitrary anchor. Real-year replay is unaffected. See [ADR 0008](../adr/0008-weather-sources.md). | M4, with the drought scenario |
| E6 | FAO-56 says Eq. 52 (Hargreaves) should be checked against Penman-Monteith in each new region before it is trusted. Paddock uses it because CliFlo stations near a farm reliably report temperature and often nothing else; the regional check against a station that does report humidity and wind is outstanding, and belongs with the T3 validation gate. | M2, with T3 |
| E8 | **No unfertilised Canterbury reference series.** T3 validates the seasonal *distribution* against Woodlands, the only site on DairyNZ's sheet with no nitrogen fertiliser - but Woodlands is Southland (46.4 S), not Canterbury (43.5 S). The Canterbury sites are the right climate and the wrong management (154-330 kg N/ha). Neither is both. A proper target is an unfertilised Canterbury trial such as the Winchmore dryland series; until then the magnitude comparison carries this caveat. | M3 |
| E7 | **The pasture growth parameters are fixtures, not a calibration.** Radiation use efficiency, extinction coefficient, specific leaf area, senescence and residual are the right shape and order of magnitude, but the published ranges are wide and often whole-plant (RUE near 2 g DM/MJ usually includes roots; this model grows only what an animal eats). **T3 now measures the gap rather than assuming it.** Over twenty simulated years the model produces 9.8 t DM/ha/yr against 11.0 t measured at the unfertilised Woodlands site and 16.3 t at fertilised Lincoln, and its seasonal curve correlates 0.97 with the unfertilised site and 0.91 with the fertilised one. The shape is right; the magnitude is 11% low against the closest-managed site. No growth figure from this model should be quoted until the parameters themselves are sourced. | M2 shape done, magnitude M3 |
| E9 | **The terrain model had never run.** `Farm::set_slopes` had no callers anywhere in the repository and `FarmletGrid::set_terrain` had callers only in tests, so two sourced pieces of the model - the energy cost of walking a slope (TMC Eq. 23) and the radiation a slope receives (Gillingham et al.) - never fired in a single scenario the project shipped. Every farm ran flat, and no report said so. A `[terrain]` section now reaches both, and the report states which ground a run was over. **All three shipped bundles now run on measured ground**, all on one tile of 2023 LiDAR at 1 m. The two demonstration bundles were moved to get there: their grid origin was a round-number coordinate that converts to central Christchurch, so the ground under a bundle calling itself a farm would have been a city. They now sit on farmland west of Lincoln, chosen from the LINZ parcel snapshot rather than from a map - 93.8% of sampled points fall inside parcels over 10 ha, 14 parcels touch the block, one under half a hectare, and three of the holdings are Rural Sections. Their weather and soils remain placeholders; only the ground is measured. The validation suites run them with the ground taken away, because those suites link no geospatial stack and their pinned numbers were set on flat ground - see `tests/support/ShippedBundle.hpp`. Synthetic surfaces remain the thing the topography code is checked against, because their derivatives are known. A run states in its report which of the two it was over. See `tests/validation/TerrainReachesTheModelTest.cpp`, which asserts direction and reachability and never magnitude | M3 |
| E20 | **CLOSED. Bought feed is zero and the floor is Beef + Lamb's, seasonal.** The cover floor was an admitted round number and a dairy one; it is now the three figures Beef + Lamb give for a ewe flock, applied by date: about 800 kg DM/ha from day 70 of pregnancy, 1,000 three weeks out from lambing, never below 1,200 through lactation. Flattening those upward to a single 1,600 had the farmer buying feed through autumn to defend a cover the ewes did not need, and bought feed substitutes for grazing rather than adding to it. With the seasonal floor and the finished lambs (E28), bought feed is 0 in all three years where it was 126 tonnes in the driest. Utilisation 21% to 46% in a dry year. **The stocking rate was never the problem**: 7.0 SU/ha against Beef + Lamb's Class 6 at 7.74. **What remains is not a defect but a missing feature** - see E38 | M4 |
| E21 | **CLOSED against the trial's own 25 years, not against reviews of them.** The Winchmore irrigation trial's pasture production is on Figshare under CC BY 4.0; `scripts/winchmore-fetch.py` pulls it, checks its hash and rebuilds `data/calibration/winchmore-annual-production.csv` byte for byte. **Fetching it corrected the target twice.** The dryland mean is 6,442 kg DM/ha where figures derived from the reviews gave 5.5 to 6.5 t - the mean sat at the very top of the derived band - and the measured range is 3,904 to 9,845 where the derived band was one tonne wide. A dryland Canterbury year is a distribution two and a half times wide, and a validation built on the derived band would have failed the model for having weather. Irrigation adds 50 to 81% rather than "roughly doubling"; the 80% the reviews quote is the wettest of four treatments. **Measured against it, this model's decade averages 8,156 kg DM/ha at 783 mm against Winchmore's 6,442 at 745** - about 20% high once rainfall is allowed for, with every individual year inside the measured range and the decade swinging the way a rain-fed farm swings. See E40 for why that 20% is not closed by re-fitting | M2 |
| E26 | **CLOSED, the cause was arithmetic, and the rate is now FITTED to a stated outcome.** A third to a half of the standing dry matter was dead where a grazed New Zealand sward runs 10 to 20%. The dead pool settles where senescence in equals decomposition out, so the dead share is s / (s + d) - and the sward file carried both at 2% a day, which forces **exactly half dead** whatever else the model does. Neither was chosen for that; they were two independent placeholders that happened to be equal. Tripling and quintupling the stocking barely moved it, which ruled out utilisation before any code changed. **No measured disappearance rate for New Zealand standing dead has been found**, and what is published is the outcome rather than the rate: rotational grazing "circumvents the accumulation of dead matter", low-stocked swards leave "greater potential for dead material to accumulate", a dry autumn pasture "may contain predominantly dead material". So the rate is fitted to that outcome and labelled fitted - 0.12 a day, about eight days in the standing crop, giving 3 to 16% dead through the year - and a validation test holds it there so the fit is checked rather than asserted. It remains the most sensitive figure in the sward and the first to replace if a measurement turns up | M2 |
| E27 | **CLOSED. Senescence is a season now, and cover is in a real Canterbury range.** Cover bottomed in September and peaked in February where a Canterbury farm bottoms out in the summer dry, and the cause was a senescence rate that was a constant: 2% a day in January and 2% a day in July. It is now leaf lifespan - DairyNZ's three-leaf rule, a tiller carrying three live leaves and pushing out a new one every so many degree-days, so the oldest dies on the same schedule - scaled by temperature **and moisture**, which is how DairyNZ state it and which matters: a thirsty tiller takes longer to make its next leaf, so a Canterbury summer rotation is longer than a spring one rather than shorter. Cover now runs 1,300 to 2,800 kg DM/ha where a New Zealand sheep farm runs 1,200 to 2,500; it was 750 to 5,300. **Two sources disagree about the interval and both are recorded**: DairyNZ's "8 days in mid-spring" gives 60 degree-days a leaf at Lincoln's measured October mean, and the published phyllochron for Italian ryegrass is 120. 120 is used, because DairyNZ's audience is irrigated fertilised dairy pasture - running the model at 60 killed leaf faster than the stock could graze it, handed the animals 6% of what the farm grew, and sold the flock down to 44 head | M2 |
| E21 | **A reference series for Canterbury dryland pasture now exists, which E8 said it did not.** The Winchmore irrigation trial ran four dryland replicates of sheep-grazed ryegrass/white clover on Lismore stony silt loam beside irrigated treatments from 1951 to 2018, at 745 mm mean rainfall (range 491-949) - the longest such trial in the world, and its 96,000 records are on Figshare under doi:10.6084/m9.figshare.13530209. Irrigation roughly doubles production there, adding 5.2 to 5.5 t DM/ha, which puts the dryland treatment near 5.5 to 6.5 t. Its companion fertiliser trial gives a nil-phosphorus long-term mean of 5,400 kg DM/ha against about 12,000 fertilised. **Not yet downloaded or wired to a test**: it is named here so the next person calibrating does not repeat the search that concluded there was nothing | M2 |
| E22 | **CLOSED, twice over: lambs graze, and now they grow.** They were born, counted, priced and sold without taking a mouthful; they have a mob and a species file of their own, and the udder is an energy transfer so the farm is never fed twice. **Then they weaned at 17 kg**, and later 23, where Beef + Lamb New Zealand say a top flock weans "at least 40kg lamb weaned per ewe mated" - at this flock's 132.3% that is 30 kg a lamb. The cause was one target for two phases: a suckling lamb and a finishing one were fed the same rate. From Beef + Lamb's 40 kg and this flock's own birth weight and dates, a suckling lamb is grown at 235 g a day, which sits under the 300 they name as what a ewe must be fed to put on her lambs - a peak rather than a mean, so a derived mean below it is the consistent reading. Weaning weight is now 30.7 kg, and the test checks the whole-flock figure rather than the per-lamb one because it cannot be met by a heavy lamb out of a poor lambing | M4 |
| E25 | **Milk crosses the udder at its net energy, and is then used at the wrong efficiency.** The ewe is charged for the net energy she puts in a kilogram of milk (TMC Eq. 46) and the lamb is credited with exactly that, so no energy is created - but the manual would have the lamb use a milk diet at km = 0.85 (TMC Eq. 5) where this model applies the pasture diet's 0.70. A suckling lamb is therefore getting slightly less out of its milk than the manual intends, which pushes weaning weight down rather than up. The fix is a per-mob diet carrying `milk_fraction`, which `DietQuality` already has a field for and nothing yet sets | M4 |
| E23 | **Ewe milk composition is the weakest number in the lactation chain.** TMC Eq. 46 turns milk fat and protein into the energy in a kilogram of milk, and the manual's own cross-reference for both is broken - it sends the reader to section 5.8, which is the monthly distribution of milk yield and carries neither. So they come from the literature: protein 5.8%, commonly reported for sheep, and fat 7.0%, which is mid-range against published values running from 4.6% in Iraqi Kurdi to 12.6% in Dorset ewes. No New Zealand Romney figure has been read. Milk energy moves about 0.38 MJ/kg per point of fat, so a Dorset flock would be some 40% dearer to milk than this says, and no absolute lactation figure should be quoted until a New Zealand measurement replaces it | M4 |
| E24 | **The shipped flock calendar described a pregnancy nine days shorter than a sheep has**, and nothing caught it because nothing read the mating date until the pregnancy term did. 1 April to 20 August is 141 days; sheep gestation runs 142 to 152 across breeds, averaging 147, with Romney at 147 to 150, and OVERSEER uses 150. Mating moved to 23 March and `FlockCalendar::invalid_reason` now refuses an interval outside the published range. **Recorded because of the first version of that check**, which admitted 136 to 164 and would have passed the very dates that motivated it - a validator that certifies the bug it was written for is worse than none | M4 |
| E28 | **CLOSED. The farm finishes its lambs, and three separate things had to be true before it could.** The crop now splits three ways at weaning - replacements, finishers, stores - under `FlockRates::finished_fraction`, so a store farm is the same flock with one number changed. Getting there turned up three defects. **The drafting rule was routed to `sell_oldest`**, which takes the oldest cohorts: a lamb draft would have sold the farm's breeding ewes. **The finishing mob was fed for maintenance**, because `Farmer::manage` sets every mob to the one farm policy every day and overwrote the finishing target before the mob ate - the lambs held 17 kg from December to May, gaining 1e-7 kg a day, and the mob now carries a target the policy will not touch. **And the target was derived against the wrong date**: 100 g/day from weaning to the end of the farm year, where the farmer sells the tail on 1 May, so the lambs arrived at 36 kg two months after they were wanted. 139 g/day over the 151 days that actually exist gets them to 38 and drafted. Store lambs are also priced on what they weigh now rather than on a finished lamb's carcass. Drafting income $50,216, balance up from $30,232 | M4 |
| E29 | **Cross-validation of this session's own work, and what it caught.** Four findings, all in code committed within the hour. (1) `Farm::paddock_green_kg_dm_per_ha` was added, its only caller reverted, and it shipped as dead code - the seventh instance of the pattern ADR 0014 names, and the first that was mine. Removed. (2) The check that stops a bundle's grid overriding its soil had no test at all; a validator nothing exercises is in the same family as one that admits the bug it was written for. Now tested both ways. (3) `Flock::step` advanced ages by one per call rather than by the days that passed, so a caller that stepped scattered dates aged its flock by the number of calls - correct in the run loop, wrong everywhere else, and the milk share of a lamb's diet is a function of exactly that number. The date is now the authority. (4) **Re-enabling the drafting rule put back the bug it had been disabled for, within the hour**: `is_finishing_class` was set to a flat `true` while `liveweight_kg` was still read off the front cohort, which is the oldest, which is a 55 kg ewe - so the rule drafted the breeding flock and closed the year with an empty farm. The flag and the weight now come from the same cohort. **The lesson is the one the flag's original comment already carried**: a boolean and the number it governs cannot be set from different places | M4 |
| E30 | **The nitrogen cycle was open at the animal, and the budget closed anyway.** Nitrogen left with the grazed dry matter as an outflow and never came back, so a grazed farm ran its soil down to nothing while the conservation test stayed green - which is exactly what an outflow with no matching inflow does. Excreta now returns: urine to the mineral and urine-patch pools, dung to the litter, and bought feed's nitrogen with them, because on this farm in a dry year half of what the stock eat came through the gate. **This is what made leaching possible at all**: in New Zealand pastoral farming it is excreta, not fertiliser, that leaches | M4 |
| E31 | **CLOSED for the inter-patch half; the rest is still stated.** Leaching counted urine patches only, where OVERSEER reports patch and inter-patch apart and puts the second - dung, fertiliser, soil organic matter mineralising - at "less than 15% of total N loss" on a grazed block. Both pools now leach with drainage, and the report prints the two apart with the inter-patch share beside them, because they are not the same problem: patch leaching is what stocking and grazing move, inter-patch is mostly what the soil does on its own. **The one parameter is FITTED to OVERSEER's own statement of the answer** and labelled so - a first attempt at one eighth put inter-patch at 60% of the loss and tripled the farm's leaching, which is what an unmeasured parameter does when nobody checks where it lands. Still not counted, and still said in every printing: runoff, direct deposition into water by stock, artificial drainage, and any attenuation between the root zone and a waterway | M4 |
| E32 | **Leaching per millimetre of drainage falls as the year gets wetter** - 0.146, 0.097, 0.087 kg N/ha/mm across the dry, ordinary and wet years - and that is the model behaving, not misbehaving: nitrate accumulates in a year with no drainage to flush it, so the little water that does drain leaves concentrated. It is also why the reports put that column before the total. A council reading only the leached column would read management into what is weather | M4 |
| E33 | **The economics file was read by nothing, and that is eight.** Every operating cost in `data/economics/canterbury-sheep.toml` came from Beef + Lamb New Zealand's Class 6 survey and was cited line by line - then typed by hand into the tests that needed it, so the twenty-two figures a farm's year rests on lived in two places and only one of them had a source. `load_economics` now reads it and `business_from` assembles a running farm out of it and a bundle, which is what let the dashboard have a money panel at all. **The pattern ADR 0014 names has now been found eight times**, twice within an hour of writing the code, and the count is itself the finding: careful sourcing and a loader are separate acts, and this project keeps doing the first and forgetting the second | M5 |
| E34 | **A dashboard is the easiest place to undo this project's discipline, so every indicator carries its own trust.** A tile reading 27.3 kg N/ha is read as a measurement; it is a model output resting on a placeholder patch uptake and excluding about 15% of the real loss. So `Provenance` travels with each indicator into the page, into the CSV export and into the comparison, a note is required wherever the trust is fitted or placeholder, and a panel of its own counts how many of the rest rest on evidence - 12 of 15 on an unpriced run, 16 of 23 on a priced one. **The flock's age structure in `business_from` is an assumption and is stated**: a bundle gives a head count, so the head is split evenly across the breeding ages, where a real flock is heavier in the young classes because the old ones have been culled out of it. Flocks need a file of their own | M5 |
| E35 | **Nine, and this one was caught twenty minutes after it was written.** `DashboardSeries::reference` was added to carry the level a trend is read against - the cover a farmer holds the farm to - set in `build_dashboard`, and read by nothing: not the text page, not the CSV, not the chart. It is used now: `SeasonChart` draws it dashed in the line's own colour and widens the axis to reach it, and the CSV carries it in the column heading, because a cover column exported without its level is a column somebody plots and then guesses at. **What is worth recording is the check rather than the fix.** Grepping for fields that are written and never read now finds these before they settle, and it found this one in code committed the same hour. ADR 0014 names the pattern; the count is at nine | M5 |
| E36 | **A test can pass for a reason that is not the reason it was written for, and this one did for a fortnight.** The drought scenario met M4's acceptance because an inflated cover figure crossed a threshold, not because a farm ran out of feed. It was not caught by the test, which was asserting exactly what M4 asks for; it was caught by fixing an unrelated caveat and watching the acceptance fall over. **What would have caught it earlier is the check that was eventually done anyway** - reading the sward composition behind the number, where 50% dead would have been visible at once. The lesson is not about drought: it is that a headline metric agreeing with its target says nothing about the quantities underneath it | M4 |
| E37 | **Three defects in a row, each hidden behind the one in front of it.** The farm would not finish its lambs, so E28 looked like a data question - which fraction to finish. It was not. The drafting rule sold ewes; fixing that revealed that the lambs never reached draft weight; fixing the target revealed that the farm policy overwrote it every day before the mob ate; fixing that revealed the target had been derived against the wrong date. **None was visible until the one in front of it was gone**, and the only reason they came out at all was instrumenting the mob and reading its demand, intake and gain day by day rather than inferring from the farm total. A stack like this is the argument for probing the level below the symptom on the first pass instead of the fourth | M4 |
| E38 | **The farm conserves feed now, and what it found out is more interesting than the feature.** A wet spring's surplus is cut, loses dry matter going into the stack and coming out of it, and is fed before anything is bought - so a good year's growth can reach a bad year's stock. The wet year cuts 630 kg DM/ha and holds 510; the dry year cuts nothing, which is the behaviour a conservation policy exists to show. **This farm has no early-spring surplus at all**: its mean cover peaks at about 1,850 kg DM/ha through the silage window and does not reach 2,300 until December, because a Canterbury dryland flock at 7 SU a hectare eats the spring flush as it arrives. NZGA put high-quality silage at "harvest pasture by the first week in November", so this farm's surplus arrives after the quality window - it is a hay farm, not a silage one. **Three things are not modelled and are stated**: the trigger compares a farm MEAN against a decision a farmer makes on a paddock's PRE-GRAZING cover, which is a category error a better constant cannot fix (the mechanism is per-paddock, shut up what the rotation left behind); everything cut is valued as the silage NZGA measured, so a January hay cut is flattered; and the stack does not survive a run, so a wet year cannot yet feed the dry year after it | M5 |
| E39 | **Cutting cleans a rank paddock up, and the model shows it without being told to.** A mower takes green and dead together where an animal takes green and leaves the residual, so a cut removes the standing dead as well - and the wet year's farm closed $2,349 better off with conservation on than off, on a year where no feed was bought either way and nothing was fed back out of the stack. The gain is the sward: 630 kg DM/ha of it left as a mixture, and what regrew behind was greener. Worth recording because it was not designed in, it fell out of taking dead and green in the proportions they stand | M5 |
| E40 | **The model grows 20% more than a FERTILISED trial while applying no fertiliser, and that gap is a missing mechanism rather than a parameter.** Winchmore's dryland treatment gets 250 kg of single superphosphate a hectare a year; this farm applies none, and its only nitrogen is clover fixation and what arrives in bought feed. An unfertilised farm should produce **less** than that trial, so the measured 20% excess understates the real gap - the trial's own nil-phosphorus control, irrigated, ran at 5,400 kg DM/ha against about 12,000 fertilised. **Re-fitting radiation use efficiency downward would close the number and hide the reason**: there is no phosphorus limitation in this model at all, and pushing a missing nutrient into a radiation parameter is exactly what the `fitted` status exists to expose rather than absorb. The honest fix is a phosphorus term; until there is one, no absolute production figure from this model should be quoted for an unfertilised farm without this beside it | M2 |
| E41 | **The release shipped two commands that could not run on anything in it, and the packaging test passed.** `attach_elevation` demotes a scenario whose LiDAR snapshot is absent to flat ground and hands back a sentence to show somebody - that is its documented contract, and the map window and the report path both honour it. `paddock dashboard` and `paddock nitrogen` printed the sentence and then exited 1. Snapshots are fetched and never shipped, and all four scenarios in the archive name one, so both commands failed on every scenario in every release; they only ever worked on a machine that had already fetched LiDAR, which is a developer's. **The reason nothing caught it is the interesting half.** The packaging script did run the staged binary, and its comment claimed that proved a missing snapshot draws flat - but the command it ran was `scenario run`, which builds a farmlet, a point model with no ground under it at all. The check would have passed with the terrain handling deleted. A test that exercises a different path than its comment claims is worse than no test, because it is counted. Fixed: both commands warn and carry on, and the smoke test now runs `dashboard` over a bundle whose snapshot is absent | M5 |
| E42 | **Three data directories were missing from every release, and the NOTICE told people to run scripts that were not in the box.** The packaging script carried an allow-list of `data/` directories - every one that existed the day it was written - so `data/diseases` went missing from M4 onward and `data/economics` and `data/regulations` from the day they were added. It is a deny-list now, keyed on what may not travel rather than on what may, and `DataFilesTest.EveryDataDirectoryIsShippedOrExplicitlyExcluded` fails when a directory is neither shipped nor named. Separately, `NOTICE.txt` directed the reader to `scripts/nz-elevation-snapshot.py` while `scripts/` was not staged - so the only route to measured ground was documented and unavailable. The fetch scripts ship now: they are ours, standard library only, and about 40 kB | M5 |
| E43 | **What may be shipped is settled by licence and needs re-checking before each release.** verify.md item 7 has the finding: LINZ elevation and cadastre are CC BY 4.0 and *may* lawfully travel, and are kept out only because they are bulk that a hash and a fetch script reproduce. NIWA's DataHub licence forbids passing data to third parties and Manaaki Whenua S-map Online is CC BY-NC-ND 3.0 NZ, so neither may ever be committed or shipped, whatever their hashes say. **The exclusion is therefore by directory, not by file**: `data/snapshots/` is the drop zone for all of them and the strictest licence in it has to govern the whole. The risk this leaves open is a licence changing under a source that is currently permissive; nothing in CI would notice, and `scripts/check-data-licences.sh` enforces the policy rather than verifying the terms | M5 |
| E44 | **The application fetches its own ground now, and only its own ground.** Every download arrived drawing flat - snapshots are never shipped - and the fix was "run a Python script", which asks somebody who wants to look at a farm to install a language runtime first. A bundle now records where its tile is published beside the hash it must have, so `paddock ground fetch` and a **Fetch ground** button do it in one request against a known address, checked before the file is put anywhere. **Verified against the live service**: 34 MB from the Canterbury Selwyn 2023 collection, hash matching what lincoln-lurdf pins. **What is not tested is the download itself** - a test that pulls 34 MB from a public bucket fails when a runner has no route out and passes for reasons unrelated to this code, which is a flaky gate and this project does not keep those. What is tested is every refusal that happens before a request goes out. The residual risk is a pinned URL going stale as LINZ revises a collection; the failure is legible and the fix is the search script, which is what it was before. See ADR 0015 | M5 |
| E45 | **The packaging script was committed in a state where it could not execute at all, and every gate passed.** Fixing E41-E43 involved checking that the new test failed when it should, which meant editing the script and putting it back - and the revert went through PowerShell's `Set-Content -Encoding utf8`, which writes a byte-order mark. Three bytes in front of `#!` and the kernel no longer finds an interpreter: `ï»¿#!/usr/bin/env: No such file or directory`. Format, line-endings, licences, dependency direction and 556 tests all passed on a file that would not run. **Nothing executes the packaging script except a release**, which is the same blind spot E41 was about, found again one commit later by the same method - running the thing. Closed twice over: `scripts/check-line-endings.sh` now refuses a byte-order mark on any committed text file, and the release is now built and unpacked by hand before the work is called done. A stray mark had also been sitting in tests/CMakeLists.txt for several commits, harmlessly, because CMake ignores them | M5 |
| E46 | **Twenty commits accumulated four compiler errors and thirty-five static-analysis errors that no local run could see, because the branch was never once built anywhere but Windows.** CI covers three platforms and it never ran: it triggers on pull requests, and the pull request was opened at the end. The four gcc errors were real portability faults - `std::int64_t` is `long long` on Windows and `long` on Linux, so a lambda returning `numeric_limits<long long>::max()` in one branch and a `days_since_epoch()` difference in the other type-checks on one platform only; and three range-for loops taking `const std::string&` over a braced list of string literals, which MSVC accepts and gcc's `-Wrange-loop-construct` does not. The thirty-five were `bugprone-unchecked-optional-access` on `ASSERT_TRUE(x.has_value())` followed by `*x`, which clang-tidy cannot see through because the assert returns from inside a macro. **None of this needed CI to find.** WSL has gcc 13.3 and builds the whole core with the same warnings-as-errors in about two minutes, which is now the rule rather than an afterthought - see the working agreements in CLAUDE.md. What CI is still needed for is clang-tidy, whose findings depend on the standard library being parsed: the local MSVC compile database makes the optional check silently produce nothing, so a clean local run proves less than it appears to. **And the obvious fix was wrong**: `.value()` is a checked access that throws rather than reads undefined memory, so it looks like the answer, and the check flags it exactly as it flags `operator*` - it wants a guard it can follow, not a safe accessor. Thirty-one of the thirty-two came back unchanged on the next run. What works is `tests/support/ValueOf.hpp`: one function with one explicit `if`, small enough for the analysis to reach the end of it, which also turns a wrong assumption into a named test failure instead of undefined behaviour | M5 |
| E47 | **The indicators page existed for weeks and nobody could find the way into it, which is the only test of a control that matters.** The button was there - bottom right of the readings panel, beside the report buttons - and styled `quietAction`: transparent, `%DIM%` text, and a disabled rule painting both the text and the border in `%SURFACE%`, the colour of the card it sits on. So before a run finished the way in was not dim, it was **gone**, and after one it was three unlabelled words in a corner. Quiet was meant to say *secondary*; it had come to mean invisible. Fixed with a surface of its own, full text colour, a disabled state that is faint and still present, and the word "Open:" in front so the row reads as a set of things you can open rather than as a caption. **Nothing in the test suite could have caught this** - it is not a wrong pixel, it is a control nobody sees - and the only reason it was found is that somebody went looking for the feature and could not find it | M5 |
| E48 | **The years page was one line chart, and a `colour_standing` parameter that coloured nothing.** Two faults from the same habit of writing what a page ought to show without checking what it does show. (1) Ten years of one series overlaid on `SeasonChart` looked right and was not: that chart draws the *first two* lines only, each on its own axis scaled to itself, because it was built for two different quantities - so eight years were silently absent and the two that were drawn were on scales that made a 5,675 kg year and a 9,779 kg one look alike. It has a `Scale::Shared` mode now: one axis, one range, every line. (2) The bar charts took a `colour_standing` flag which was passed, stored, branched on, and called `QBarSet::setBarSelected()` - which does not change a bar's colour. Qt colours a *set*, never a bar within one, so every bar stayed accent blue and a page whose whole point was showing which years crossed a threshold showed nothing of the sort. **Instance eleven of the same pattern**: a quantity computed, plumbed and read by nothing. Now one set per standing, stacked, so five years over the nitrogen trigger are five red bars | M5 |
| E49 | **The window ran the farm without its books, so the flock never moved, and every flock figure on screen was the truth about a farm with no stock on it.** `keep_the_books` is what advances the year - it charges the day, ages the flock, records the lambing and the culls and the weaning draft, and hands the grazing mob its head count back - and it only runs when a `FarmBusiness` is present. The command line took one through `--economics`. The window never loaded any. So the map drew 417 ewes while the indicators page reported a closing flock of **zero**, for every year, for as long as that page existed. **Utilisation was the tell and I read it as a model defect**: 13% where a New Zealand sheep farm runs 65 to 80%, which went into a caveat about intake. With the books loaded it is 26 to 35% - still far short, still worth chasing, but half the gap was a run that never happened. The economics are read beside the bundle now, the way every other input is, and are optional the way the nitrogen rule is: no file, fewer panels, never invented prices | M5 |
| E50 | **The documentation had drifted a long way behind the code, in the direction that flatters nobody.** README's Known Limitations claimed four gaps the code had closed - lactation, excreta return, nitrate leaching, and calibration against measured New Zealand yields - and the nitrogen model document opened by saying a grazed run strips the farm, which had stopped being true. Nine rows of the maintenance energy table still read "not yet used" for terms `AnimalEnergy` now calls on every animal on every day. **A tracker whose provenance column is stale is worse than no column**, because that column is the one a reader trusts to say what is live; and a limitations list that overstates is still wrong, however safe the direction. Both are corrected, and the closed items are listed as closed rather than deleted, because a list of limitations is only worth anything if it can be seen to move | M5 |
| E51 | **The utilisation band was quoting a number nobody had sourced, and sourcing its numerator moved the diagnosis to the other half of the model.** The dashboard read "New Zealand sheep farms run 65 to 80%" and cited nothing - the same failure E20 records for the cover floor, a plausible figure remembered rather than read. Utilisation is a ratio, so the honest fix is to source its numerator: Parker (1998), *Proceedings of the NZGA* 60: 243-248, defines the stock unit as a 55 kg ewe weaning one lamb on **550 kg DM a year**, and in the same paper states the standard unit as "550 kg DM of **10.5 MJ ME/kg DM** per annum" - which is the diet this model already feeds, so the comparison is like for like without an adjustment. **Corrected by E53 within the hour:** this first read 7.0 SU/ha and 3,850 kg DM/ha of demand, on an unsourced 1.35 SU a ewe. At Parker's 1.0 SU a ewe it is 5.2 SU/ha and 2,867 kg DM/ha, and the model's 2,434 is 85% of it rather than 63%. Meanwhile the same year grows 6,847 against Winchmore's measured dryland mean of 6,442 - inside the band, `ok`. So the gap is not production - and, per E53, not mainly intake either. `data/calibration/stock-unit-intake.csv` carries the figures and `StockUnitIntakeTest` holds the gap where it is. Parker's Table 4 also reports a STOCKPOL sheep farm at 84.6 to 89.6% utilisation on 6,233 to 6,683 kg DM/ha, recorded as the upper end rather than as a target because it is a modelled farm and the table does not state how it defines utilisation | M5 |
| E52 | **The cause of the intake gap is named and not yet fixed: there is no appetite in this model.** `Farm::step` sets a mob's demand from `target_gain_kg_per_day` - what the farmer wants the animals to gain - so a mob standing in a surplus asks for exactly what it asked for in a drought. The irrigation comparison shows it plainly: two arms differing by 5,290 kg DM/ha of growth eat 2,434 and 2,429 kg DM/ha, five kilograms apart. Real intake is bounded by what the animal can physically eat and reduced by what it can find, and CSIRO's relative-intake relationships are the published route to both. Until that exists, **no result from this model that depends on how much was eaten should be read as a response to feed supply** - which includes every irrigation comparison it produces | M5 |
| E53 | **The stocking rate was the answer, and finding it meant correcting E51 an hour after writing it.** E51 took a ewe as 1.35 stock units, which is what `data/economics/canterbury-sheep.toml` asserted - without a source - to reconcile 417 ewes on 80 ha with Beef + Lamb's Class 6 rate of 7.74 SU/ha, and concluded "the same kind of farm". Parker's Table 1 collects the published conversions: a ewe is **1.0 SU** in the New Zealand Meat and Wool Board's Economic Service figures - the survey that became Beef + Lamb's, so the same lineage as the number being matched - and 1.0 in MAF's. The modelled flock is 417 ewes at 55 kg rearing 105%, which is Parker's base ewe almost exactly. **So the farm runs 5.2 SU/ha, a third under its class, and demand is 2,867 kg DM/ha not 3,850.** The model eats 2,434, which is 85% of its rating rather than 63%. Three consequences: the intake gap is ordinary rather than alarming; the low utilisation is mostly stock the farm does not carry; and **intake capacity would not have fixed it** - a physiological cap and an availability response both move intake down, and the farm needs more animals, not hungrier ones. The 1.35 was invented to make a comparison come out, which is the failure mode this whole tracker exists for, and it was in a file whose own header boasts about marking two strengths of evidence | M5 |
| E54 | **The farm can state its stocking rate now, and doing so raised a question the benchmark does not answer.** A stock-unit rating is data on the species file - a ewe 1.0 from Parker's Table 1, a lamb at foot deliberately 0.0 because a stock unit is the ewe *and* her lamb and counting it again would carry it twice, a dry cow 0.0 because Parker's spread of 5.1 to 8.5 is too wide to pick from. The run accumulates stock-unit-days and reports the mean, because lambing nearly doubles the head and weaning takes it back. **The mean reads 3.5 SU/ha where the opening count reads 5.2**, and Beef + Lamb's Class 6 figure of 7.74 does not say which of those it is - opening, wintered, or mean. So the indicator is `verify` rather than `derived`: the arithmetic is sound and the comparison is not yet like for like. Settling it needs the survey's own definition, which is one email or one page of a methodology document | M5 |
| E55 | **The model's answer to "how many ewes will this farm carry" is "as many as you like", and that is a defect in the feed constraint rather than a missing stocking policy.** Before building a policy that sets flock size, the same farm was run at six stocking rates to see what the existing mechanism does. It responds correctly in every respect but one: production falls with grazing pressure (6,847 to 5,851 kg DM/ha), utilisation climbs into the measured band (36% to 69%), the cover floor is breached, and feed is bought. **But profit never turns down** - the closing balance rises monotonically from $34,056 at 417 ewes to $83,142 at 950, which is 11.9 SU/ha on a Canterbury dryland block, and `days short of feed` reads 0 at every one of them. The cause is that bought feed is unlimited, always available, and priced at a hardcoded `kSupplementDollarsPerKgDm = 0.42` in `core/src/FarmDecision.cpp` whose own comment claims "the feed price it is charged at is the one the economics file carries" - it is not, it is a constant with no source and no provenance marking. The only brake on stocking is solvency, so a farm that can pay can farm without grass. **A stocking policy built on this would optimise against a constraint that does not bind**, which is why one was not built; what is needed first is a feed market with a price that has a source, a quantity that can run out, and a season when it is not for sale | M5 |
| E56 | **The stocking ladder is a result worth keeping, not just a diagnostic.** The example farm at its own 417 ewes returns $34,056 and uses 36% of what it grows; at Beef + Lamb's Class 6 rate of 619 it returns $62,771 and uses 57%, which is inside the derived band, while breaching the cover floor and buying $8,465 of feed. That is a real trade a consultant would want to see, it is the first thing this model has said that a farmer could act on, and it took six runs and no new code. It also shows the machinery works: the farm responds to stocking through pasture, cover, purchases and money, all of it already wired. What is missing is the brake at the top end (E55) and a way to run a ladder without copying bundles by hand | M5 |
| E57 | **A Waikato farm was being measured against the Canterbury Land and Water Regional Plan, because the window reached for whatever rule was on disk.** `paddock nitrogen` has always taken the regulation as an argument and refused to default one, on the stated grounds that reaching for a rule asserts which zone a farm sits in - and the Canterbury file's own header warns that two farms a valley apart can differ. The window did exactly what the command line refuses to: it loaded `regulations/canterbury-nitrogen.toml` and `economics/canterbury-sheep.toml` for every bundle opened, so `ruakura-fe` was priced from a South Island survey and reported against a South Island rule. **A scenario names its own now**, the way it already names its species and its sward, and both are hashed like every other input so a compliance figure is reproducible. The two Canterbury bundles name Canterbury; ruakura-fe names neither and says why in the manifest, so it reports what leached and makes no compliance claim. Held by a test | M5 |
| E58 | **Diet quality was a constant in six places, and it is the input every animal calculation starts from.** `10.5 MJ ME/kg DM` and `75%` were written into MapWindow.cpp three times and main.cpp three times, so a scenario could choose its sward, its soil, its species and its management but not what its stock got out of the grass. It is a bundle field now, defaulting to the same 10.5 - which is not arbitrary: Parker (1998) defines the stock unit as "550 kg DM of 10.5 MJ ME/kg DM per annum", so it is the density the country's stocking arithmetic is written in, and the reason this model's intake can be compared with a stock-unit rating at all. **What it still does not do is vary.** Real pasture runs roughly 9.5 to 11.5 across a season and falls as dead material builds up; this model tracks green and dead per cell and feeds neither back into what the animals are getting. That is a closed loop in the sward and an open one at the animal, and it is the next thing to close after the feed market of E55 | M5 |
| E59 | **The setup panel had seventeen controls against about two hundred configurable values, and all seventeen were one person's.** A farm consultant, a researcher and somebody writing a consent report open this simulator wanting different things, and a single flat list served none of them: a researcher could not reach radiation use efficiency or diet quality, a compliance reader could not see which rule applied. **The two parameters those roles most needed were the two that were hardcoded** - E57's regional rule and E58's diet quality - which is not a coincidence: with no role owning a parameter, nobody noticed it had no home. The panel now switches on who is looking. Farmer is what was there. Researcher adds the model's own assumptions, with diet quality as its first control - placed second in the panel rather than fourth, because in the researcher view it is the point and below the fold is how E47 happened. Compliance shows which rule this farm is read against, and says plainly when none is, which is the honest answer for a farm nobody has told us the rules for. The role changes which controls are shown and nothing about the run | M5 |
| E60 | **The feed price was a constant claiming to come from a file it had never been in.** `kSupplementDollarsPerKgDm = 0.42` sat in `core/src/FarmDecision.cpp` under a comment reading "the feed price it is charged at is the one the economics file carries". It was not. It is now Beef + Lamb New Zealand's Fact Sheet 260 figure - baleage at **54.1 c/kg DM**, from their own worked example: a 450 kg bale at 37% DM is 166.5 kg DM, and $90 a bale gives 54.1c. **The line above it in their table is the one that matters most**: pasture, 10 c/kg DM. Bought feed costs this farm five and a half times what the same feed costs it standing in the paddock. A farm with no price stated now refuses to buy at all, rather than buying at a figure nobody can check - which a unit test caught within the minute. **The price is not the brake, though**: re-running the six-rate ladder at 54.1c instead of 42c, profit still rises monotonically to $104,419 at 950 ewes. E55's conclusion stands, and the feed *market* - a quantity that runs out, a season when there is none for sale - is still unbuilt and now correctly sequenced last | M5 |
| E61 | **The model's annual total is right and its seasonal shape is badly wrong, which is exactly the failure an annual comparison cannot see.** The Winchmore sheet is called "Monthly, seasonal, and yearly total production" and `winchmore-fetch.py` was reading the TOT column and discarding the other sixteen. Extracting them cost nothing - same download, same hash - and the comparison it makes possible is damning:<br><br>`winter 6.2% vs 10.0% · spring 32.2% vs 54.7% · summer 41.3% vs 18.0% · autumn 20.3% vs 17.3%`<br><br>**The model's biggest season is summer. On a Canterbury dryland farm summer is when growth stops.** More than half the trial's year is spring; less than a third of the model's is. Meanwhile the annual total, 6,847 against a measured 6,442, sits comfortably inside the band `CanterburyDrylandTest` checks - so every validation this project had was passing a model that grows its year in the wrong order. The measured columns also show what an annual mean hides: dryland summer production ranges from 3,208 kg DM/ha to **zero** - 1981-82 grew nothing at all across December, January and February. `WinchmoreSeasonalTest` holds the gap from both sides rather than loosening a band; the aspirational version is in the file's history. First place to look is summer growth continuing on a soil that should be dry - water stress, available water, or rooting depth - and it is the same family as E40: too little water limitation and no phosphorus limitation are two independent reasons to grow too much | M5 |
| E62 | **The water stress was working. It is the senescence that is not, and the two halves of drought cancel almost exactly.** E61 named three suspects for the summer growth that will not stop - the stress coefficient, the profile's available water, the rooting depth. All three are innocent. Measured over the run, Ks averages **0.618 in spring and 0.366 in summer**, with 86 to 100% of November-to-February days below 0.5: summer is correctly the more limited season. The fault is that `core/src/Pasture.cpp:147` feeds the same water factor into leaf death, in the direction that *protects* the sward - `effective = degree_days * water_factor`. A thirsty tiller pushes its next leaf more slowly, so the leaf it is carrying lives longer. That is true, DairyNZ state it, and it is why a Canterbury summer rotation is longer than a spring one. **It is also only half of what a drought does.** The other half is that drought kills standing leaf outright, and the model has no term for it. Senescence share is linear in degree-days times Ks, so the cancellation can be read off exactly: spring 7.5 x 0.618 = **4.64**, summer 12.5 x 0.366 = **4.59**. The model's sward loses leaf at the same rate in a February drought as in an October spring - a 78-day leaf lifespan in both. So green cover never falls (2,006 kg DM/ha in October, 2,154 in February), light interception holds, and growth carries on. A real dryland sward browns off. The rest follows arithmetically: summer potential is **2.06x** spring (temperature factor 1.67 x radiation 1.24), water knocks it back to 0.59 of that, giving 1.22x against the 1.28x observed - and the 0.33x measured. **Flagged while here:** `optimum_temperature_c = 20.0` is a PLACEHOLDER carrying real weight, since it makes February at 17.5 C the most favourable growing month of the model's year. Lowering it would improve the seasonal shape for the wrong reason and is not the fix. The fix is a drought leaf-death term with a source behind it | M5 |
| E63 | **The missing half of drought is now in, sourced, and it is worth a seventh of the seasonal error - and it made the farm hungry for the first time.** E62 named the mechanism; this is what implementing it did. The form and all three figures are APSIM AgPasture's `MoistureEffectOnTissueTurnover` - below a Ks of 0.6 the tissue turnover rate is multiplied by `1 + 1.0 * ((0.6 - Ks) / 0.6)^2`, doubling on a profile at wilting point - with the values its own ryegrass and white clover both carry, from `AGPRyegrass.json` and `AGPWhiteClover.json`. The model is Li FY, Snow VO & Holzworth DR (2011), NZJAR 54: 331-352, which is calibrated on exactly this question. **Adding it alone did almost nothing** - spring 32.2% to 32.4%, summer 41.3% to 41.3% - because the leaf-appearance term was still dividing by the same Ks, so the two halves of drought went on cancelling and summer turnover rose only from 0.99x spring's to 1.14x. Removing the water factor from leaf *appearance*, which is what AgPasture does (its turnover is temperature times moisture, and moisture *increases* it - there is no term anywhere in AgPasture where water slows leaf death), moved every season the right way: `winter 6.2>6.7 (10.0) · spring 32.2>35.6 (54.7) · summer 41.3>39.9 (18.0) · autumn 20.1>17.9 (17.3)`, and the decade mean from 8,106 to 7,287 kg DM/ha against a measured 6,442. **Three points of a twenty-two point gap.** Six sevenths of it are still there and the next suspect is named below. **The result that matters more than the shape**: `DroughtDestockingTest` used to assert that a drought year and a wet year harvest within 5% of each other, and said in the comment that separation would mean the farm had become feed-limited. They separated - 155 tonnes eaten against 195, and the year's lowest cover moved out of late winter and into the summer, 652 against 954 kg DM/ha. **This is the first time anything in this model has gone short of feed for a reason the weather caused**, and it is the thing an intake model has to stand on. Nobody sells yet, so M4's destocking acceptance is still unmet | M5 |
| E64 | **The optimum temperature was never the problem. The shape around it was, and both are now sourced as one set.** E64 was opened suspecting `optimum_temperature_c = 20.0`, a PLACEHOLDER carrying the model's whole summer advantage. A reader checking the literature found the opposite of what the suspicion implied: Oregon State extension gives perennial ryegrass a growth optimum of 65-70 F (18.3-21.1 C), classic summaries give 18-20 C, and a 2022 Frontiers in Plant Science study of multiple accessions gives 25.2-29.4 C with large genotype variability - so **20 C is a well-supported figure and the placeholder happened to be right**. Their more important point: an optimum is meaningless without the process it belongs to (leaf elongation, shoot growth, germination, biomass accumulation are not the same optimum) and without the response function it sits inside, and a triangular response from 4.4 C to 20 C was the thing worth doubting. That is what was changed. The whole set now comes from APSIM AgPasture's C3 photosynthesis response - `TemperatureLimitingFactor`, after Thornley & Johnson - which is a biomass-growth process on a New Zealand-calibrated model: ryegrass `Tmin 2.0, Topt 20.0, q 1.5` and a maximum of 32.0 derived as AgPasture derives it (`Topt + (Topt - Tmin) / q`), clover `3.5 / 23.0 / 1.65 / 34.82`. **Topt did not move.** | M5 |
| E65 | **A photosynthesis response was being asked about the wrong temperature.** Having adopted AgPasture's curve, Paddock fed it the plain 24-hour mean - half of which is spent in the dark at the coldest part of the day. AgPasture evaluates it twice, at the daily mean and at a daylight temperature of `0.75 * max + 0.25 * min`, weighted one to three. The error this causes is proportional to the diurnal range and to how steep the curve is where you are on it, so it is worth **30% in a Canterbury winter and 2% in its summer** - which is to say it was suppressing exactly the seasons that were too small. Correcting it is not a new degree of freedom; it is the rest of a method already adopted | M5 |
| E66 | **What E63-E65 bought, and what is left: winter and autumn are closed, spring and summer are not.** Three sourced changes, none of them tuned: `winter 6.2>10.5 (10.0) · spring 32.2>38.7 (54.7) · summer 41.3>33.5 (18.0) · autumn 20.3>17.3 (17.3)`. **Winter and autumn now land within half a point of the trial**, where they were four and three points out, and their test bounds have gone from twelve points to two and a half. Spring and summer moved six and eight points and are still sixteen apart. The annual figures moved with them: the radiation use efficiency was refitted to the water use efficiency it was always fitted to - Martin et al. (2006), 12.3 kg DM/ha/mm - and lands at **12.24**, with the decade mean at **6,572 kg DM/ha against a measured 6,442**, inside 2%. **That last number undercuts E40's evidence**: the model used to grow 13-20% more than the trial while applying no phosphorus, and most of that excess turns out to have been the temperature response rather than the missing nutrient. E40 is not closed, but the number it rested on is gone and it needs a new argument. **What remains is a spring/summer partition problem and nothing else**, which is a more tractable question than the one this started as. Unexamined: spring phenology and reproductive growth, tillering, seasonal nitrogen supply, and whether the water balance really empties a Canterbury profile in February. Not to be closed by moving a cardinal temperature | M5 |
| E67 | **Spring dominance is a property of the plant and the model treats it as a property of the weather. That is the whole of what is left.** E66's five candidates were measured and three are dead: spring is not water-limited (Ks **0.930**, six of ninety-one days below half), spring is not nitrogen-limited (95-102 kg mineral N/ha, and the pool *accumulates* all year, 67 to 168), and AgPasture's heat stress would not bite (6.8% of summer days above its 28 C onset, mean excess 1.5 C - about 1.4% of the season). **The discriminating measurement**: in Winchmore's 25 measured years spring beats summer **25 times out of 25**, median ratio 3.14, never below 1.09. The model manages 8 of 10, median **1.16**, and loses outright in exactly the years summer stays wet - 2021-22, summer Ks 0.899, summer 3,912 kg DM/ha against spring's 2,882. So the model's answer to "when does grass grow" is "whenever there is water", and the record's answer is "in spring, every year, whatever the summer does". **It shows up as two missing tails.** The model's summer runs 1,370-3,912 (mean 2,305) where the trial's runs 0-3,208 (mean 1,158) - *the model's worst summer in ten years beats the trial's average one* - and the model's spring runs 1,826-3,265 (mean 2,651) where the trial's runs 1,936-5,785 (mean 3,523), so *the model's best spring is below the trial's average one*. And the peak growth week lands in December or January in six years of ten, where the trial's biggest month is October by a distance (1,588 kg DM/ha, 52% ahead of November and 3.3x December). **Leading hypothesis, not yet demonstrated: there is no reproductive development in this model.** Perennial ryegrass turns reproductive in spring - stem elongation and seed head are much of the October flush - and afterwards carries fewer tillers and less growth potential through summer even with water in the soil. Paddock has no phenology at all: growth is light x temperature x water x nitrogen, so it grows when conditions allow, and in Canterbury the best conditions are in January. Nothing here can be fixed by moving a parameter | M5 |
| E68 | **E64 made one field serve two processes, which is the same mistake one level down.** `base_temperature_c` moved from 4.4 to 2.0 as part of adopting AgPasture's photosynthesis parameter set - correctly, for photosynthesis. But `senescence_share` reads the same field for the thermal time a tiller needs to push out a leaf, and leaf appearance is a different process with its own base temperature. Senescence is linear in `T - base`, so the change raised it by **32% in spring and 19% in summer** - penalising, of the two, the season that was already too small. It was invisible because it landed inside a change that improved three seasons out of four. The fix is a separate leaf-appearance base temperature, and it is not to be added until there is a figure to put in it: the phyllochron literature uses bases from 0 to 4 C and this model should not pick one by preference. Related: `degree_days_per_leaf = 120` is already marked VERIFY, having been set at twice the 60 that DairyNZ's eight-days-in-mid-spring implies, because at 60 the leaf died faster than the stock could graze - which is the same coupling seen from the other end | M5 |
| E69 | **The missing phenology now has the same model's published stand-in for it, and it is worth a third of what was left.** E67 identified reproductive development as the mechanism this model lacks. AgPasture has a device for exactly that, and is candid about what it is: "Reproductive phase of perennial is not simulated by the model, the ReproductiveGrowthFactor attempts to mimic the main effect, which is a higher allocation of DM to shoot during this period." **Every number in the curve is a function of latitude**, which is what separates adopting it from fitting a seasonal multiplier to the trial it is checked against - nothing in it was chosen by looking at Winchmore. At -43.641 it opens on **1 September**, ramps 35.8 days to a plateau on **7 October**, holds 59.6 days, and tapers 23.9 days to **29 December**, peaking at **+28.3%** shoot allocation. Winchmore's biggest months, in order, are October, November, September, December. Applied to potential growth before nitrogen has its say, because it is an allocation change rather than a change in what the plant fixes; Paddock has no root pool to take it from, and its RUE is already an above-ground one. Result: `winter 10.5>9.1 (10.0) · spring 38.7>43.6 (54.7) · summer 33.5>32.2 (18.0) · autumn 17.3>15.1 (17.3)`. **Spring gains five points and autumn pays two of them**, which was predicted before the code was written and is held by a 2.5-point bound autumn now sits 2.2 inside. **It does not restore the signature**: spring still beats summer 8 years in 10 against 25 in 25. A 28% spring boost cannot outweigh a summer that grows 3,900 kg DM/ha when the rain comes, and the remainder wants the mechanism rather than another multiplier | M5 |
| E70 | **The two sourced targets have come apart, and that is worth more than picking one.** RUE was fitted to water use efficiency - Martin et al. (2006), 12.3 kg DM/ha/mm - and the Winchmore annual total is what the model is *validated* against. After E64 both agreed: WUE 10.98 and a decade mean of 6,572 against 6,442. Adding the reproductive season moved WUE to **12.43**, one percent from its fitted target and better than it has ever been, while the decade mean went to **7,547**, seventeen percent above the trial. **RUE was left alone**, because re-pinning it to the annual total would be fitting the model to its own validation and would leave nothing to check it with. The consequence is that E40's argument has now moved twice in one day: the production excess it rested on was 13-20%, fell to 2% when the temperature response was sourced, and is back to 17% with the reproductive season in. **It is not evidence for a missing phosphorus limitation and it never was** - it is a number that moves whenever anything upstream of it moves, and E40 needs an argument that does not depend on it | M5 |
| E71 | **Intake capacity is half built: the mechanism is sourced and tested, and it cannot be switched on until a milking ewe has an appetite.** E52 named the gap - a mob ate its whole requirement until the grass physically ran out, which is a cliff where grazing is a curve, and two irrigation arms 5,290 kg DM/ha apart in growth ate five kilograms apart. Both halves of the fix now exist, from GrazPlan (Freer, Moore & Donnelly; CSIRO Plant Industry Technical Paper, the same CSIRO lineage the OVERSEER manual cites): **potential intake**, Eq. 2, `Imax = C_I1 * SRW * Z * (C_I2 - Z)`, and **relative intake**, Eqs. 16-17, a rate term that falls as the bite gets smaller times a time term that lets an animal graze up to 1.6 times as long to compensate. **The parameter table does not survive text extraction with its columns aligned, so every value used was confirmed against something else in the paper**: C_I2 = 1.7 because the text says the quadratic peaks at a relative size of 0.85 and 1.7 is the only value for which it does; C_R5 = 0.6 because Fig. 4's upper line starts at 1.6 and equation 17 gives `1 + C_R5` on bare ground; and the pair `0.04, 1.7` because Fig. 2's 50 kg sheep peaks near 1.44 kg DM a day, which they reproduce. **The pairing is the whole thing**: the availability term is below one at any cover a real farm carries - 0.90 at 2,000 kg DM/ha - so it must multiply an appetite, which for a dry 66 kg ewe is 1.85 kg DM a day against a requirement nearer 1.2. It then bites only below about 800 kg DM/ha, which is right. **It fails for a lactating female**, whose appetite GrazPlan raises through its LF factor (Eq. 8-13) - and LF needs body condition, which this model does not carry, and a peak-intake parameter that could not be read reliably and has no independent check in the paper. Wired into `Farm::step` without it, a ewe with a lamb at foot was capped below her own requirement, went short every day of lactation and was sold: the flock fell away from 332 head and the farm ate **558 kg DM/ha against 2,026**. So the functions, their data and their tests are shipped and nothing grazes through them yet. Next: body condition, then LF | M5 |
| E72 | **Found on the way, and worse than the gap that found it: this model's ewes eat like cattle and grow to 80 kg.** Probing the grazing loop before changing it, the main mob runs 57.1 to 61.9 to 71.0 to 76.7 to **80.6 kg** across one year against a standard reference weight of **66 kg** - 22% over mature size - with intake reaching **2.6 kg DM/head/day**. Some of that is legitimate: 2.6 kg is about right for a ewe in full lactation. Ending the year 22% above mature weight is not, and the bundle asks for none of it - `target_liveweight_gain_kg_per_day = 0.0`, so the farmer is feeding these ewes to hold weight and they are gaining 23 kg anyway. `Farm.cpp` already carries a comment about a ewe that went from 55 kg to 101 over a year and was fixed; this is the same fault, smaller and still there. **It inflates everything downstream** - the farm's intake, its utilisation, its stocking headroom and the 68% stock-unit figure of E63 are all measured against an animal a fifth larger than it should be. Not caused by E71's work and not fixed by it; found because switching on a physiological ceiling is what makes an animal without one visible | M5 |
| E73 | **The 80 kg ewe was one bug, and it is fixed: the energy ledger was only balanced in one direction.** `daily_energy_requirement` charges a ewe for her milk and for the lamb she is carrying. `liveweight_response` deducted neither - it took intake, subtracted maintenance, and turned everything left into body tissue. So every megajoule she ate *in order to lactate* came back out as fat. Fed exactly the requirement the manual computes for her, a ewe at 14 days lactating gained **0.184 kg a day**; over a hundred-day lactation that is 18 kg, and E72's ewe put on 23. The two functions are one ledger read forwards and backwards and they now agree. Both regression tests were confirmed to fail with the deduction removed | M5 |
| E74 | **The second half - making gain an outcome rather than an instruction - was built, measured, and not shipped.** `target_liveweight_gain_kg_per_day` runs backwards through the energy model to set a feed demand, which is the wrong causal direction: a farmer cannot instruct an animal to put on 80 grams. What a farmer controls is stocking rate, rotation length, grazing residual, what is fed out and when stock are sold; weight is what those choices produce, and money is what the weight produces. Replacing the instruction with GrazPlan's appetite was tried and measured: the flock ate **1,434 kg DM/ha against 2,026**, which is the right direction - the old figure was inflated by E72's oversized ewe - but lambs weaned at **16.7 kg against a validated floor of 20**, and **22.0 kg a ewe mated against the 32 Beef + Lamb ask of a top flock**, so the change moves the model away from measured data and is not shippable. Mature ewes came out at zero net gain over the year in both arms, which for a breeding ewe is defensible and which also means the target-gain lever had stopped doing anything. **Applying the availability term on top collapsed the farm to 148 kg DM/ha**, for E71's reason. What the refactor needs before it can land: GrazPlan's lactation factor so a milking ewe has an appetite of her own, its condition factor so a mature animal stops eating when it is fat, a look at why a lamb on an understocked farm cannot grow on its appetite, and management rules keyed to observed liveweight - sell at a weight, not at a date | M5 |
| E75 | **Intake capacity is live, and what unblocked it was giving a ewe in milk an appetite of her own.** E71 shipped the availability equations and could not switch them on: the term sits below one at any cover a real farm carries, so against a bare requirement it put every lactating ewe permanently short and the flock sold itself down. The missing piece was GrazPlan's lactation factor, and the parameter it needs - `C_I19,n`, the peak by number of young - could not be read from the technical paper, whose table loses its column alignment in extraction. **APSIM ships GrazPlan's own parameter file**, `Models/Resources/ruminant.prm`, and it settles every value: `c-imx-` for wool breeds, which is where GrazPlan files the Romney, gives **0.524, 0.524, 0.707, 0.891** for none, one, two and three lambs. Those are the paper's own sheep figures times the 0.8 its footnote applies to the group - 0.6553, 0.8843 and 1.1143 scale to 0.5242, 0.7074 and 0.8914, agreeing to four figures. **The same file independently confirms every parameter this project had already inferred from the paper's figures**: C_I1 0.04, C_I2 1.7, C_R4 and C_R6 0.00112, C_R5 0.6. It also gives C_I20 = 1.5 for the condition factor and C_N1-3 for normal weight. So appetite now carries all three of its terms - size, condition and lactation - and a ewe at peak lactation wants **1.52 times** what a dry ewe wants, about 2.8 kg DM a day against a requirement nearer 2.6. That is the headroom the availability term needs. Wired into the farm the flock now eats **1,908 kg DM/ha against 2,026**, a 6% reduction, and all 586 tests pass including the Beef + Lamb weaning validation. E71's blocker is closed | M5 |
| E76 | **Relative size is a frame, not a weight, and the first cut of this had it wrong.** GrazPlan's Z is normal weight over standard reference weight, and its BC is liveweight over normal weight; the first implementation here used liveweight over SRW for Z and had no BC at all. The difference is Eq. 1a, which lets normal weight go on rising through a season an animal does not gain in - so a ewe comes out of a drought **light for her frame rather than small**, and can catch up. Without it a hard winter would have permanently shrunk an animal's appetite. Both equations are now implemented and need no new state: N is a function of age and current weight, and both are already carried. The condition factor is the other half - past a relative condition of one a ewe's appetite falls away, **not to stop her eating but until what she eats is what holds her**, so a well-fed animal settles at a weight instead of growing without limit. Nothing in this model told a mature animal it was big enough before | M5 |
| E77 | **The target gain is bounded on the grass and unbounded at the trough, so it is still an instruction.** E75 put appetite and availability into the paddock, and grazing is now limited by what an animal can physically harvest. Bought feed is not: it is capped at what the mob is being fed *for*, so a farmer who asks for half a kilogram a day buys his way there. Asked for 0.5 kg/day the flock returns **182.49 kg over a year against a target of 182.5** - to the decimal, which is arithmetic rather than simulation. No ewe does that. It is E55's finding from the animal's end rather than the accountant's: bought feed is the one intake nothing bounds, which is also why profit rises with every extra ewe. **The obvious fix was tried and is not shipped.** Capping the trough at the animal's appetite does bound the target - the year comes back at 163 kg, and asking 0.1 kg/day returns 32.6 rather than exactly 36.5 - but it destabilises the farm: a lactating ewe's appetite runs only about **8% above her requirement** while the availability term takes **10%** at ordinary cover, so she runs a small permanent deficit, loses 3.6 kg over a year where she used to hold, and the farmer destocks into it. The flock's grazed intake fell from 1,908 to 256 kg DM/ha. **Two things this entry first said are wrong and are corrected here.** It said GrazPlan's omitted LA and LB terms would raise a milking ewe's appetite and so supply the headroom. They would not: Eq. 10 is `LA = 1 - C_I15 + C_I15 * BC_at_parturition` with C_I15 = 0.5, which is **exactly 1.0** for a ewe lambing in normal condition, and LB only ever reduces LF. A body-condition history would not close this. It also attributed the destabilisation to the trough cap; switched on alone the cap is **inert** in both scenarios measured - the impossible target still returns 182.49 kg and the flock still eats 1,907.74 kg DM/ha - so the collapse came from something else in that edit that was never isolated. **What is actually known**: bought feed is unbounded, the target is therefore an instruction, and the cause of the earlier collapse is open. **The one real lever found**: GrazPlan's lactation peaks are grouped by breed - wool 0.524, crossbred 0.59, meat 0.655 - and it files the Romney under wool, at a standard reference weight of 55 kg. This project's ewe is a New Zealand Romney at 66 kg from the OVERSEER manual, which is a dual-purpose animal rather than an Australian wool one, and the meat-breed column is the paper's own unscaled sheep figure. Moving between those columns changes a milking ewe's headroom over her requirement from 8% to 18%. That is a data decision with a source behind it and no New Zealand evidence yet to settle it. Held from both sides by `AnImpossibleTargetIsBoughtRatherThanRefused` | M5 |
| E78 | **Running the shipped scenarios end to end found three faults in the command line, none of which any test could see.** (1) `--help` listed two commands and the binary dispatches six - `dashboard`, `nitrogen`, `disease` and `ground fetch` were undiscoverable, and writing them up found that the argument list for `nitrogen` is a bundle and a *regulation file*, not weather. (2) **`paddock dashboard` ignored the economics its own bundle names.** Every shipped scenario carries an `[economics]` entry and the command read one only from `--economics`, so the farm ran without its books and the flock never advanced: the Lincoln bundle reported **940 kg DM/ha eaten at 13.5% utilisation** where its own economics give **1,908 kg at 28.7%**. The GUI had this exact fault and it was fixed there; the command line kept it. (3) **`paddock scenario run` simulates one ungrazed hectare**, and it is the one command the help has always carried. A bundle describes a farm - mobs, a grazing calendar, paddocks, a grid, rules and prices - and this command reaches for none of it, so the same Lincoln bundle grows 7,219 kg DM/ha here and 6,645 through `dashboard`. The figures are right and they answer a different question from the one the bundle poses, which is worse than being wrong. Not restructured, because the regression baselines are pinned to that output; it now names its own scope and points at the command that runs the farm | M5 |
| E79 | **What the four shipped scenarios actually report, which is the first cross-comparison of all of them.** `canterbury-baseline` has no stock at all, so the dashboard declines it - it is a pasture bundle rather than a farm. Of the three that carry stock, **utilisation is outside its band in every one** (20.4%, 28.7%, 6.9%) and so is stocking rate (3.55, 2.77, 5.21 SU/ha against Beef + Lamb's Class 6 at 7.74), which is E20 seen three times over. Water use efficiency is inside its band on both Canterbury farms (12.8 and 12.6 kg DM/ha/mm against Martin et al.'s 12.3) and outside on Ruakura at 21.1 - which is close to that paper's *irrigated* figure of 20 and may be the band's fault rather than the model's. **Ruakura is the outlier and should be read with care**: 18,975 kg DM/ha grown against 1,301 eaten is 6.9% utilisation, a farm that grows nineteen tonnes and harvests one. It exists to carry a facial eczema climate, not a stocking policy. And the two `canterbury-*` bundles run on a synthetic generator whose own file says its parameters "have not been taken from NIWA's published normals for any" site: they draw 721 and 926 mm against a Lincoln normal near 630. **The only shipped scenario standing on real weather with a validated pasture model is `lincoln-lurdf`** | M5 |
| E80 | **Three of the four scenarios were still running the pasture model this project spent a week correcting.** E62 to E69 sourced a drought leaf-death term, a published temperature response and a reproductive season, and every one of them reached `lincoln-lurdf` alone. The other three carried byte-identical copies of the sward file it started from: a radiation use efficiency of **1.5 against the fitted 0.639**, a triangular temperature response on placeholder cardinals, no drought term and no reproductive season. That is why Ruakura grew nineteen tonnes of dry matter a hectare. **The sward is now one file** - `data/pastures/ryegrass-clover-nz.toml` - because these are properties of the plants and not of a farm, and what legitimately differs between them (weather, soil, latitude, stock) is stated by the scenario. **And the two `canterbury-*` bundles now read twenty-five years of real Selwyn weather** fetched from Open-Meteo for the plains their LiDAR tile sits on, in place of a generator whose own file said its parameters were never taken from anyone's published normals. Both read the same file on purpose, so grazed against ungrazed is a comparison rather than two unrelated runs. Result: `canterbury-baseline 9,589>8,284 · canterbury-grazed 10,248>8,368 · ruakura-fe 19,186>13,004 · lincoln-lurdf 7,219 unchanged`. **Ruakura at 13.0 t is an out-of-sample prediction** - the radiation use efficiency was fitted at Lincoln and has never seen Waikato - landing inside the 12 to 16 t published for that region, and Lincoln unchanged is the control that says nothing else moved | M5 |
| E81 | **Correcting the scenarios broke seventeen tests, and every one of them was resting on the old farm.** Worth listing because the pattern is the finding. Three cover assertions held `> 1000 kg DM/ha`; the corrected farm bottoms at 640-770, and that low is not the stock eating it out - it is **769 at 400 head and 733 at 1,200**, a 36 kg spread across a threefold stocking range, so the winter trough is senescence rather than grazing. Two 'feed to spare' premises became unreachable: on a rotationally grazed dryland farm a confined mob meets a day its paddock is short of it at every stocking rate, so the buy rule never rests - it buys 31 kg DM a head at 100 and 61 at 300, which is the rule answering need and is now what the test asserts. The seasonal comparison against DairyNZ's Woodlands diverges in five months and only five: below in **February, March and April** and above in **October and November**, which is a Canterbury dryland year against a Southland one, so the test now asserts that direction instead of a tolerance that would have said nothing. Correlations fell from 0.90 and 0.85 to **0.82 and 0.75** while keeping the ordering the gate exists to state. The golden baseline was re-pinned: mean cover 3,135 to 1,620, eaten 125 to 92 tonnes, mob moves 98 to 30. **Not one band was loosened to make something pass** | M5 |
| E82 | **A latent bug in the test harness, found because this was the first test to copy two bundles at once.** `BundleCopy` named its temporary directory after the source path and the test name and nothing else, so a second copy inside one test resolved to the first one's directory, deleted it and re-copied the pristine bundle - silently discarding every edit the first copy had made. A test comparing two differently-edited bundles compared two identical ones, and the failure pointed nowhere near the cause: the seeds read 20240701 and 20240701 after one of them had been set to 20240702. Each instance now gets its own tree | M5 |
| E83 | **E68 is closed: tissue turnover has its own temperature response, and one field has stopped doing two jobs.** Leaf death ran on thermal time above `base_temperature_c`, which is a *photosynthesis* cardinal - so when E64 sourced the growth minimum and moved it from 4.4 to 2.0, senescence came with it, up 32% in spring and 19% in summer, penalising of the two the season that was already too small. **AgPasture settles the question by stating both**: its ryegrass has `GrowthTminimum 2.0` and `TurnoverTemperatureMin 0.5`, which are different numbers because they are different processes. Turnover now uses AgPasture's own `TempFactorForTissueTurnover` - `reference * ((T - min) / (ref - min))^exponent`, **capped at the reference rate above 16 C**, with `0.05, 0.5, 16.0, 1.5` for ryegrass and `0.05, 1.0, 18.0, 1.5` for clover, from the same resource files as everything else here. The cap is what stops a warm Canterbury summer turning leaf over ever faster. **It also retires a fitted parameter**: `degree_days_per_leaf = 120` was carried at twice the 60 that DairyNZ's own eight-days-in-mid-spring implies, because at 60 the leaf died faster than the stock could graze - a number set to stop a symptom. It is now inert on every shipped sward. **What it bought, honestly**: about half a point of seasonal shape - `spring 43.6>43.9, summer 32.2>31.7` - and the decade mean from 7,547 to **7,286** against a measured 6,442, with water use efficiency at 12.0 against Martin et al.'s 12.3. The correctness was the reason; the shape was not moved by it | M5 |
| E84 | **The seasonal levers this project can source are now spent, and the gap is 11 points of spring and 14 of summer.** Five sourced mechanisms have been added against E67 - drought leaf death, a published temperature response taken as a set, a daylight temperature to evaluate it at, a latitude-driven reproductive season, and turnover's own response - and together they moved the year from `6.2 / 32.2 / 41.3 / 20.3` to `9.4 / 43.9 / 31.7 / 15.0` against a measured `10.0 / 54.7 / 18.0 / 17.3`. **Winter and autumn are within a point and a half. Spring and summer are not, and the structural signature is unchanged**: spring beats summer in 25 of Winchmore's 25 measured years and in 8 of this model's 10, losing in exactly the years summer stays wet. What is left is not a parameter. A real dryland sward comes out of its reproductive spring with fewer tillers and cannot answer summer rain the way this model does, and neither Paddock nor AgPasture carries tillers - AgPasture's reproductive factor, already adopted here, is its own stand-in for the same absence. **Closing this means building phenology**, which is a larger piece of work than anything in E62 to E83 and should be scoped as one | M5 |
| E85 | **`[irrigation]` reached the manifest, and the reason it had not is the reason it had to.** Until now the only place a run could be told to irrigate was the desktop panel - `SetupPanel` said so in as many words - so an irrigated result could be reproduced only by somebody who also had the window open and remembered which boxes were ticked. That is the same argument `[management]` already carries in `ScenarioConfig.hpp`, and it applies harder here: irrigation is the largest single lever in this model, moving Lincoln 2023-24 from 6,407 to 11,747 kg DM/ha. `ScenarioBundle::irrigation` is optional and empty means rain-fed, which is what a New Zealand farm is unless somebody says otherwise. **One command still does not honour it**: `paddock scenario run` steps a single ungrazed farmlet through `core::run`, which takes no schedule - so it now prints that it is ignoring the bundle's irrigation rule rather than reporting a rain-fed year under an irrigated bundle's name | done |
| E86 | **CORRECTED by E88: the irrigated arm is calibrated against nothing, which is true, and is not unbenchmarkable, which is what this entry implied.** `data/calibration/winchmore-annual-production.csv` has carried three measured irrigated treatments over 25 years since it was fetched, and nothing had used them; E88 does. The rest of this entry stands.** `demo-irrigation-off` and `demo-irrigation-on` share every input with `lincoln-lurdf` and differ in four lines, which `FlagshipDemoTest` checks semantically and again as manifest text. The rain-fed half is the farm this project validates on. The irrigated half is not: every calibration and validation here is against Winchmore's **dryland** series, so 11,747 kg DM/ha is a model prediction. **Two figures give it a sanity check and neither is a validation.** The marginal response is 5,340 kg DM/ha for 375 mm applied, or 14.2 kg DM/ha/mm, against Martin et al. (2006) near 20 for irrigated Canterbury ryegrass and clover on total water use - so the model is at the modest end of the published range, which is the safe direction for a demonstration. And 375 mm over eighteen events is inside the ordinary Canterbury season. **The leaching row is the one to watch**: irrigation takes the farm from 2.1 to 14.7 kg N/ha against Environment Canterbury's Selwyn Waihora limit of 15, which is exactly the trade-off this model is for and rests on coefficients marked `placeholder`. Quote the direction, not the number. Closed by E88, which does exactly that | M5 |
| E87 | **The customer-facing summary renders the tracker rather than competing with it.** `DemoSummary` picks six outcomes from the dashboard that already built them and labels each with a `Confidence` derived, in one function, from the `Provenance` and the band that indicator already carried - so a figure cannot be promoted by editing the summary. The mapping: a band plus Direct or Derived is *benchmarked*; `Fitted` is *calibrated* whatever band it has, because a value fitted to a trial and then compared against that trial is checking its own arithmetic; `Verify` is *exploratory*; `Placeholder` is *do not quote*; anything else is *conserved*, meaning read off a budget the conservation suite closes to 1e-9. **Nothing on the flagship page reaches benchmarked**, which is the true statement about this model and is why the label exists. Four outcomes are named as deliberately absent with the tracker's own reasons: bought feed (E71, E77), animal production, money, and leaching | done |
| E88 | **The residual seasonal error does not block the flagship demonstration, because the demonstration's claim is an annual ratio and that ratio is measurable - against data this repository already had.** E86 said the irrigated arm was checkable against nothing. It was wrong: `winchmore-annual-production.csv` carries `irrigated_10pc`, `irrigated_15pc` and `irrigated_20pc` beside the dryland column, 25 measured years each, and no test or report had ever read them. **The measurement**: per-year response ratios average 1.55, 1.75 and 1.91 across the three treatments. The demo waters at FAO-56's p = 0.6 - as soon as the sward would be held back - which is a wet regime, so the 15% and 20% treatments are its company: 50 treatment-years, median 1.90, mean 1.83, p10 to p90 of **1.23 to 2.46**. **The model's Lincoln 2023-24 ratio is 11,747/6,407 = 1.83, the 48th percentile of those fifty.** The demo year's 652 mm is 13% below the trial's 745 mm mean, so a ratio at or above the median is expected rather than anomalous. **But the seasonal path to that total is wrong, and measurably so.** Splitting the run: the rain-fed arm grows `10.4 / 53.2 / 26.7 / 9.6` against Winchmore's measured `10.0 / 54.7 / 18.0 / 17.3` - in this particular year spring is nearly right and the error is summer-over at 1.49x with autumn under by 7.7 points, which is a different shape from E84's decade mean and is one year, not a trend. **68.3% of the irrigation response falls in summer** (3,679 of 5,383 kg DM/ha), which is exactly the season the model overstates, because summer Ks goes 0.381 to 0.999 and E67's finding is that this model answers summer water in a way a real post-reproductive sward cannot. So the bias does not cancel in the difference - it is concentrated in it - and the annual total is right partly through compensating errors. **What that permits and forbids**: the annual response is defensible because it is now benchmarked; any claim about *when* the feed arrives is not, and that includes the monthly and daily illustrations. Phenology is **explicitly deferred** rather than blocking: it would change the seasonal path and there is no evidence it would move the annual ratio off the measured median it currently sits on | done |
| E89 | **`Lowest cover` was reported as Fitted and is fitted to nothing.** Fitted means calibrated so that published observations reproduce; no measured minimum-cover series has been found, the band it carries is the farm's own `minimum_cover_kg_dm_per_ha` management setting rather than an observation, and a third to a half of what it counts is dead material whose disappearance rate has no New Zealand measurement behind it either (E26). Read through `DemoSummary`, Fitted renders to a customer as "reproduces published observations", which is false. Downgraded to **Verify**, which renders as *exploratory*, and the note now says what the band is: outside it means the farmer's floor was breached, not that the model disagrees with a measurement. Found while auditing the demo page's claims against E63-E84 | done |
| E90 | **The irrigation response is gated, and it is the first external check this project has on what irrigation does rather than on what a rain-fed farm produces.** `IrrigationResponseTest` runs both flagship halves through the same priced path the demonstration uses, takes `Pasture grown` off the dashboards the customer reads, and requires the ratio to sit in **1.25 to 2.40**. That band is the 10th to 90th percentile of Winchmore's 15% and 20% soil-moisture treatments over fifty treatment-years - 1.23 to 2.46 - rounded **inward**, which is the strict direction: a gate a fraction tighter than its sample can raise a false alarm and can never wave a bad model through. **The distribution is read, not transcribed**: `tests/support/WinchmoreResponse.hpp` builds the per-year ratios through the existing `CalibrationTable`, and a test re-reads the file and fails if the two constants stop sitting inside the percentiles they came from or drift more than a rounding away from them. **Result**: rain-fed 6,407, irrigated 11,747, ratio **1.833**, the 48th percentile of the measurement, 14.25 kg DM/ha per mm applied against Martin et al. (2006)'s ~20 - which is a ceiling here rather than a band, since a model converting water faster than the published figure is over-responding whatever its ratio says. **No parameter was changed.** The ratio was 1.796 on first run because the test used the unpriced path; E79 already recorded that a bundle run without its economics is a different farm, and correcting the test to the demonstration's own configuration gave 1.833 without touching the model. **The percentile is reported and not gated**: fifty treatment-years from one trial on one soil is a sample, and a pass condition on a rank would claim more of it than it can carry. **The seasonal split is not gated**, per E88 - it carries a regression guard at 26.4% today against a 32% ceiling and a 15% floor, which asserts nothing about the world and exists so that a change which moved the season could not pass unremarked | done |
| E91 | **The demo page's strongest claim is now derived rather than asserted.** `DemoSummary` carries an `Irrigation response` row whose band is `kIrrigationResponseLow/High` and whose status comes from `confidence_of` reading that band exactly as it reads every other indicator's - so the page renders *benchmarked* because the evidence supports it and there is no second place where that word could be typed in. It is the only benchmarked row on the page; everything else is calibrated, conserved or exploratory. The absolute production figures stay **calibrated**, because radiation use efficiency was fitted to a published water use efficiency; it is the ratio that has never been fitted to anything and is therefore the one thing here measured independently | done |
| E92 | **E77 is closed on the architecture: bought feed is bounded by what the animal can hold, and offering is no longer the same act as eating.** The chain is now capacity, then trough within it, then grass within what is left, then weight out of what was actually eaten. What was missing was one bound: supplement was capped at the mob's *demand*, and the demand carries `target_liveweight_gain_kg_per_day`, so a farmer who asked for half a kilogram a day bought his way there. `MobDay` now carries `supplement_offered_kg_dm` beside `supplement_kg_dm` and only the eaten half reaches a budget, so "we fed out and they could not eat it" is a state the model can represent and report. **Why E77 found the cap inert**: it was applied to a quantity the demand bound had already limited, and - the larger reason - `potential_intake_kg_dm` returns zero for every sheep in this project, so the guard fell through to the requirement and the cap could not bite whatever it was applied to. See E93. `IntakeCausalityTest` covers the eight cases with the parameters set explicitly: abundant pasture, pasture-limited with and without feed, feed offered past appetite, an impossible target, a lactating ewe against a dry one, condition closing appetite as a ewe fattens, and the invariant that pasture plus trough never exceeds capacity. **Shipped output is unchanged** - 661 tests pass and the flagship reports the same figures - because the ceiling is switched off in the sheep data | done |
| E93 | **`appetite_scalar_per_day` has been 0.0 for both sheep since the day appetite was added, marked `status = "direct"`, and that switched the entire GrazPlan intake ceiling off for every sheep in the project.** Every caller guards the value and falls back to the animal's energy requirement, so it failed silently and exactly as designed to: E71's and E75's measurements - "intake capacity is live", "the flock now eats 1,908 against 2,026" - were taken against a number that never reached the file. Cattle carry 0.025 and work; `AnimalEnergyTest` sets 0.04 by hand and passes; the ewe file's own commentary says "at this ewe's 66 kg reference weight and full size that is 1.85 kg DM a day". Three agreeing sources for a value written as zero. **Switching it on is blocked on evidence, not on code, and the measurement is now on record**: with C_I1 = 0.04 the ceiling is 1.85 kg DM/day at 66 kg, and at day 14 of lactation this ewe's demand is 2.61 kg DM against a capacity of 2.59 - one per cent short - with the availability term taking a further ten per cent at an ordinary 2,000 kg DM/ha. She cannot physically eat her requirement at peak lactation on a good sward, loses weight, and the farmer destocks into it: the flock runs to nothing, the farm eats 589 kg DM/ha against 1,908, and seven animal validations fail. That is E71's collapse and E77's measurement reproduced with numbers. **The value is left at zero and its status corrected to `placeholder`**, which is what a zero standing in for 0.04 is; marked `direct` it told every reader this was GrazPlan's number. The one lever with a source is E77's breed column - GrazPlan files the Romney under wool at 0.524 where the meat column is 0.655, worth 8% against 18% of headroom - and there is no New Zealand evidence to settle it, so it was not moved to make anything pass | M5 |
| E94 | **The sheep appetite gap is not a coefficient problem. It is two lactation curves from two model families, two weeks out of phase.** Audit of the intake chain, no parameter changed. **What the equation is**: GrazPlan Eq. 2 gives `Imax = C_I1 x SRW x Z x (C_I2 - Z)` and that is the intake of a **dry, mature animal in normal condition** - a baseline before physiological state, not a whole-animal capacity. Reconstructed for the shipped ewe at C_I1 = 0.04: `0.04 x 66 x 1.0 x (1.7 - 1.0)` = **1.848 kg DM**, which settles the question the species file's own commentary raises - **the quoted 1.85 kg is baseline dry intake, not peak-lactation capacity**. State multipliers then apply: at peak lactation LF = 1.525 and capacity is **2.817 kg DM**, agreeing with E75's "about 2.8". Condition enters as CF (GrazPlan Eq. 3), which is correctly suppressed while lactating; there is **no pregnancy multiplier and there should not be** - GrazPlan puts pregnancy in the energy demand, not in the intake ceiling. **Nothing is applied twice.** Availability enters once, on grazing only, which is the correct semantics: `relative_intake` constrains how much of the ceiling can be filled from pasture and does **not** reduce the ceiling, and supplement is bounded by total capacity rather than by capacity x RI - so bought feed correctly bypasses the availability constraint while staying inside total appetite (E92). **The real defect**: the ME demand runs on OVERSEER TMC Eq. 35, whose curve `AnimalEnergy.cpp` itself notes "peaks around day 14", while the appetite runs on GrazPlan's Eq. 8 with `lactation_peak_days = 28`. Demand peaks a fortnight before appetite does. Measured across the curve at 2,500 kg DM/ha: day 1 capacity 1.88 against 1.88 needed, **day 7 2.25 against 2.58 - short**, **day 14 2.59 against 2.71 - short**, day 21 2.77 against 2.65 ok, day 28 2.82 against 2.52 ok, and comfortable thereafter. **Two days in ninety, and the flock destocks into them.** A real ewe does mobilise condition in early lactation, so a small early deficit is right; its size here is an artefact of mixing two source families rather than a property of a ewe, and the farmer's response to it is a management rule rather than a biological one | M5 |
| E95 | **E77's "one real lever" is aimed at the wrong parameter, and the shipped ewe mixes two animals.** E77 proposed moving GrazPlan's lactation-peak column from wool (0.524) to meat (0.655) to buy headroom, describing it as the lever for the appetite gap. Two corrections. **First, C_I1 is not breed-specific**: 0.04 is GrazPlan's sheep value, confirmed twice in this repository - E71 from Fig. 2's 50 kg sheep peaking near 1.44 kg DM, E75 from APSIM's own `ruminant.prm`. There is no ambiguity about the appetite scalar and it is **direct sourced**. The breed ambiguity is entirely in C_I19, the lactation peak. **Second, the shipped ewe is internally inconsistent about which animal it is**: its standard reference weight is 66 kg from the OVERSEER manual, a New Zealand dual-purpose ewe, while its lactation peaks are GrazPlan's wool column, which that model files the Romney under **at a 55 kg reference weight**. Half of this animal is a New Zealand Romney and half is an Australian Merino-type, and the two halves multiply. **The repository cannot settle it.** What is needed and is not here: a published New Zealand measurement of ewe voluntary dry matter intake at or near peak lactation for a dual-purpose ewe of about 65 kg - Massey or Lincoln indoor-feeding trials are the likely source - or, failing that, a New Zealand lactation curve giving the day of peak yield, which would settle the phase question in E94 without needing an intake figure at all. Until one of those exists the choice between 0.524 and 0.655 is a preference, and E77's own framing - "a data decision with a source behind it and no New Zealand evidence yet to settle it" - stands | M5 |
| E96 | **Provenance corrections from the E94 audit, values untouched.** `appetite_scalar_per_day` **unresolved**, not placeholder as E93 left it: a placeholder is an engineering value with no evidence behind it, and this one has three agreeing sources naming 0.04 and a shipped value of 0.0 held back by a known blocker. Unresolved is what a number whose evidence is settled and whose activation is not should read as. `appetite_size_coefficient` 1.7, `lactation_curve_exponent` 1.4, `lactation_peak_days` 28, `condition_intake_limit` 1.5, `normal_weight_rate` 0.0157, `normal_weight_exponent` 0.27 and the intake availability trio all stay **direct sourced** - each is confirmed against APSIM's `ruminant.prm` as well as the paper. `lactation_peak_no_young` through `_three_young` are **direct sourced but for the wrong animal**, per E95, and should be read as direct-with-a-caveat until the breed question closes. `standard_reference_weight_kg` 66.0 stays direct, from OVERSEER. **And one documentation defect**: `AnimalEnergy.hpp`'s note on `appetite_scalar_per_day` still says "four factors are missing... condition (CF) ... and lactation (LF)", which E75 implemented - so the header understates what the model does. Not corrected here because this task changes no code | M5 |
| E14 | **Facial eczema's season is a month or two early, because there is no litter term.** The model fires December to February and is finished by March; DairyNZ gives January to May, with autumn typically the worst of it. The cause is structural rather than a mis-set rate: sporulation here depends on temperature and rain alone, and *Pseudopithomyces chartarum* grows on the dead matter at the base of the sward, which is why the disease is an autumn one - the litter has to accumulate first. Adding a substrate term needs a litter accumulation rate nobody located, and guessing one would move the season by construction rather than by mechanism. `high_risk_months` sits in the data file unread for the same reason: clamping the season to the published months would hide this rather than fix it | M4 |
| E15 | **Rain is monotonically favourable, and it should not be.** The sporulation test asks for 4 mm over 48 hours and treats anything wetter as at least as good. Marbrook and Matthews (1962), *New Zealand Journal of Agricultural Research* 5:223-236, found sporidesmin is rapidly eluted from spores by water, so heavy rain washes toxin off pasture that a drizzle would have left on it - the model reads a downpour as worse than a drizzle where the literature says the opposite. **No longer blocked**: since E19 closed, the dose axis is toxin-days and elution has something to act on. What is still missing is a rate against rainfall | M4 |
| E16 | **Sunlight destroys the toxin and the model does not know.** The same paper: on an overcast day there was no detectable loss of sporidesmin from either the exposed sample or the darkened control, and on the following bright clear day "all the sporidesmin was altered". With E15 that is a coherent mechanism for why the dangerous weather is warm humid drizzle under cloud - the drizzle elutes toxin without washing it away, and the cloud spares it. **One reason left.** E19 closing gave it a toxin state to act on, but the measurement is of an aqueous extract in a tube, not of toxin on pasture where much of it is still inside spores: applying "one bright day destroys all of it" to a paddock would mean facial eczema could never build through a sunny Waikato summer, which is the opposite of what happens. The direction is published, the magnitude is published for a tube, and the transfer to a sward is not | M4 |
| ~~E19~~ | ~~**The toxin is computed and read by nothing.**~~ **Closed.** Exposure now accumulates toxin-days per gram rather than spore-days, so `toxin_ng_per_g` and the Fitzgerald et al. (1998) conversion behind it are load-bearing. The fitted reactor exposure is 2115 ng-days/g, which is the exact translation of the 1,500,000 spore-days it replaces at 1.41 pg per spore - **the move changed no published result on the day it happened**, and the ten-year comparison reproduces to the digit. What changed is what the conversion can do: `AnAtoxigenicPastureDamagesLessThanAToxigenicOne` puts the same spore count on toxigenic and atoxigenic pasture and asserts the damage differs by about the 2.7 ratio the paper measured, which was unreachable while the axis was spores. E15 and E16 now have a toxin state to act on; they remain open on their own merits, chiefly that the photolysis magnitude was measured on an extract in a tube | M4 |
| E17 | **Toxin per spore is a constant, and DairyNZ says it is not.** "Given that young spores contain more toxin, pastures are more toxic when conditions promote sporulation" - so a count of 50,000 on the way up is more dangerous than the same count on the way down, and this model charges both the same 1.41 pg. The direction is published; the size is not | M4 |
| E18 | **The clinical fraction ignores dose, and is applied to the wrong denominator.** `clinically_affected` returns a tenth of the mob for any GGT at or above the reactor threshold, so a mob at 56 IU/L and one at 6,001 report identical visible cases - and the real proportion rises steeply with dose. DairyNZ's "about 10% of *affected* animals show clinical signs" is also a share of the affected, not of the mob, so applying it to head count assumes every animal is affected. Both need a dose-response curve that has not been located | M4 |

| ~~E10~~ | ~~**The activity term is fed by nothing.**~~ **Closed.** `Farm::conditions_on` now supplies Hkm and Vkm from the mean slope of the ground a mob is on, so TMC Eq. 24 is charged in every run. The distances are published rather than chosen: OVERSEER *Characteristics of animals* v6.3, Table 30, taken from Nicol and Brookes (2007, Appendix - activity costs), giving 0.5/1.0/1.5/2.0 km horizontal and 0/0.1/0.15/0.2 km vertical for flat, rolling, easy hill and steep hill. **The note that this was blocked on open item 9 was wrong**, and the error is worth recording: item 9 asks where stock *choose* to graze on ground of varying slope, which is a distribution; Eq. 24 needs only an average distance by topography class, which Nicol and Brookes published and OVERSEER has used for years. Item 9 stays open, for a different question. What this project did have to decide is where one topography class becomes the next, since OVERSEER takes the class as a user input per block and this model has a measured slope per cell: flat below 8 degrees, rolling below 16, easy hill below 26, steep above - Manaaki Whenua's LUC slope classes and Te Ara's description of pastoral land break at the same two degrees. **The effect is measured, not asserted**: sheep maintenance moved from 15% low against CSIRO to 5%, the golden regression moved every one of its 366 days, and a year of the grazed scenario now eats 1.4% more on flat ground. See `WalkingDistanceTest` and `SheepMaintenanceAgainstAdjabuiWithGrazingIncluded` | M3 |
| E11 | **A 25 m cell was one 1 m pixel.** The GeoTIFF reader took the single DEM pixel under each output cell's centre - 0.16% of the ground the cell stands for at 25 m over a 1 m survey - on the stated reasoning that averaging is a kind of invention and the reader's job is to report what the DEM says. That was the wrong quantity: a molehill, a wheel rut or one noisy return became the height of a quarter-hectare. It showed as a visibly crumpled farm on ground that is not, and it fed the slope, which feeds the energy cost of walking (TMC Eq. 23) and the radiation a face receives (Gillingham et al.). The reader now takes the **mean of the source pixels whose centres fall inside the cell**, which is not interpolation - bilinear would invent values between measured points and is still not done - and every number in the average is a measured pixel inside the cell it is reported for. Where a cell is finer than the DEM there is nothing to average and it falls back to the pixel under the centre. Effect on the shipped ground: the Canterbury block's range moved from 10.485-17.295 m to 10.664-17.296 m, the low end being a single-pixel outlier. `tests/gis/GeoTiffElevationTest.cpp` pins it with a one-pixel spike, because a plane cannot tell the two rules apart | Done |
| E12 | **Wind is recorded and read by nothing.** `DailyWeather` carries `wind_speed_m_per_s`, `WeatherConfig` parses a monthly mean for it, both weather sources produce it and `is_valid()` checks it - and no file under `core/src` outside the weather sources ever reads it. Not `Pasture`, not `SoilWater`, not `AnimalEnergy`, not `Farm`. It is the same shape of gap as E9 and E10: an input taken seriously all the way to the point where it would matter. Two places it belongs, both needing a decision rather than a patch: evaporative demand, where this model uses Hargreaves - radiation and temperature range, no wind - rather than FAO-56 Penman-Monteith, which does use wind at 2 m; and cold stress, which is open item 12's territory. Note the shipped Open-Meteo series makes this worse before it makes it better: it supplies a daily **maximum** at **10 m**, and both the statistic and the height are wrong for either equation. Recorded rather than wired, deliberately, because guessing which equation to feed it to is how a model acquires a number nobody can defend | M3 |
| E13 | **The farmer's target gain reached only the chequebook, and now that it reaches the animals there is no ceiling on them.** Demand was computed from `AnimalState::liveweight_change_kg_per_day`, which holds what the mob did YESTERDAY, so a well fed mob ate to maintenance, changed by nothing, and was offered maintenance again: a stable fixed point that left a ewe on exactly her opening weight for 366 days while `target_liveweight_gain_kg_per_day` sat in the panel doing nothing. It only ever entered the decision about how much feed to BUY, which on a farm with grass to spare is no decision at all. Intent is now a separate field, `Mob::target_gain_kg_per_day`, set by the farmer each day; asking for 0.1 kg/day over a year now returns 36.5 kg against a theoretical 36.6. **The remaining gap is that essentially nothing damps it.** TMC Eq. 44-46 make gain dearer as maturity rises - the ewe above went from 0.85 to 1.4 of her standard reference weight - but nothing forbids exceeding mature weight, so asking for 0.2 kg/day for a year yields a 123 kg ewe and the model does not object. A ceiling needs a source for where it sits and what happens at it, which is why one has not been invented here | M3 |
| E5 | The CliFlo column mappings in `scripts/cliflo-snapshot.py` are guesses until checked against a real export of each datatype. The script prints the headings it found and accepts `--column HEADING=field`, so a wrong guess is visible rather than silent. | M2, on first real download |
